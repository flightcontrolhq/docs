---
title: "ravion git"
sidebarTitle: "git"
description: "Manage Git integrations for Ravion projects, including connected repositories, branches, webhooks, and authentication from the CLI."
"og:image": "https://www.ravion.com/og/docs/cli/reference/git.png"
"twitter:image": "https://www.ravion.com/og/docs/cli/reference/git.png"
---

{/* This page is generated from the Ravion CLI source. Do not edit by hand. */}

## `ravion git`

Manage Git integrations

**Aliases:** `gits`

### `ravion git connect`

connect opens your organization's Git connections settings page in a browser, where you can connect GitHub or GitLab and manage which repositories Ravion can access.

```bash
ravion git connect [flags]
```

**Flags:**

| Flag | Required | Description |
| --- | --- | --- |
| `--no-browser` |  | Print the URL instead of opening a browser |

### `ravion git namespaces`

List Git namespaces

```bash
ravion git namespaces [flags]
```

**Flags:**

| Flag | Required | Description |
| --- | --- | --- |
| `--json` |  | emit response as JSON |
| `-v, --verbose` |  | show full output |

### `ravion git self-hosted-get`

Get self-hosted server

```bash
ravion git self-hosted-get <id> [flags]
```

**Flags:**

| Flag | Required | Description |
| --- | --- | --- |
| `--json` |  | emit response as JSON |

### `ravion git self-hosted-list`

List self-hosted servers

```bash
ravion git self-hosted-list [flags]
```

**Flags:**

| Flag | Required | Description |
| --- | --- | --- |
| `--cursor <string>` |  | cursor |
| `--json` |  | emit response as JSON |
| `--limit <int>` |  | limit |
| `-v, --verbose` |  | show full output |

