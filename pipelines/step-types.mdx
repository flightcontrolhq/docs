---
title: "Step types"
description: "The catalog of available pipeline steps."
---

Available step types include `custom`, `terraform:apply`, `terraform:plan`, `build:image`, `build:lambda`, `build:static`, `deploy`, `approval`, `sleep`, and `trigger pipeline`.

{/* TODO: Expand this page. */}
