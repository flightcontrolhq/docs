---
title: "Pipelines overview"
description: "The DAG execution model and blocks."
---

Pipelines execute as a dependency-driven DAG. Steps run as soon as their dependencies are satisfied. Blocks group steps: **parallel** blocks run children concurrently, **group** blocks run them sequentially.

{/* TODO: Expand this page. */}
