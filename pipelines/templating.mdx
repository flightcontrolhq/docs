---
title: "Pipeline templating"
sidebarTitle: "Templating"
description: "Reference pipeline inputs, run context, and step outputs inside step configuration with Ravion's expression and templating syntax."
---

Use template expressions such as `<< pipeline.input.x >>`, `<< steps.id.output.y >>`, and `<< trigger.payload.z >>`. Expressions resolve in DAG dependency order.

{/* TODO: Expand this page. */}
