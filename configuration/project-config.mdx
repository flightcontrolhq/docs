---
title: "Project configuration"
description: "The declarative project config file: environments and module instances."
---

import ProjectConfigSchemaReference from "/snippets/schema-reference/project-config.mdx";

Define a project declaratively in `.json`, `.jsonc`, `.yaml`, or `.cue`. The top-level `environments[]` array contains `moduleInstances[]`, and modules reference each other with `moduleGivenIdRef`.

Use this reference when you author project config files or generate project configuration from another system.

For a compact, machine-readable version, fetch `https://api.ravion.com/projects/config/schema.md`.

<ProjectConfigSchemaReference />
