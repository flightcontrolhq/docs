---
title: "Module build facet"
sidebarTitle: "Build"
description: "Build container images, Lambda functions, and static assets for a Ravion module, including build commands, cache, and artifact storage."
---

The build facet covers `build:image`, `build:lambda`, and `build:static` outputs consumed by deployments.

{/* TODO: Expand this page. */}
