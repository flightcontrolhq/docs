---
title: "Module inputs and configuration"
sidebarTitle: "Inputs and configuration"
description: "Define module inputs, set default values, and configure validation so consumers can safely customize each instance from the Ravion catalog."
---

Define the input schema for a module and provide default values via default-value definitions.

{/* TODO: Expand this page. */}
