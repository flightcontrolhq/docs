---
title: "How to Deploy Angular to AWS"
sidebarTitle: "Angular"
description: "Deploy an Angular application to your AWS account with S3 and CloudFront through Ravion."
keywords: ["Angular AWS deployment", "deploy Angular to AWS", "Angular static hosting", "S3", "CloudFront"]
"og:image": "https://www.ravion.com/og/docs/deploy/angular.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/angular.png"
---

import { DeployPrerequisites } from "/snippets/deploy/prerequisites.mdx";
import { DeployNextSteps } from "/snippets/deploy/next-steps.mdx";
import { AskAssistant } from "/snippets/ask-assistant.mdx";

Deploy the Angular production build to S3 and deliver it through CloudFront. Angular CLI writes to `dist/<project-name>` by default; replace the example directory with the `outputPath` from your `angular.json`.

<DeployPrerequisites />

## Configure the deployment

```yaml ravion.yaml
project:
  givenId: angular-site
  name: Angular site
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: static-site
        name: Angular site
        type: rvn-aws-static
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: angular-site-production
          routing: spa
          default_version: main
          build_source: railpack
          source_repo: my-org/my-angular-app
          output_directory: dist/my-angular-app
          railpack_build_cmd: npm run build
          build_infrastructure_type: ec2
          build_instance_size: c7a.4xlarge
```

Run `ng build` through your `npm run build` script. Angular uses the production configuration by default. Set `output_directory` to the actual `outputPath` if your project name differs.

## Apply and connect your domain

```bash
ravion project create --given-id angular-site --name "Angular site"
ravion project config apply angular-site --file ravion.yaml --dry-run
ravion project config apply angular-site --file ravion.yaml
```

Follow [custom domains](/guides/custom-domains) to connect your domain to CloudFront.

<AskAssistant question="How do I configure Angular outputPath and SPA routing for CloudFront on Ravion?">
  "How do I configure Angular outputPath and SPA routing for CloudFront on Ravion?"
</AskAssistant>

## Next steps

<DeployNextSteps />
