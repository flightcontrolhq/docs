{/* Generated by scripts/sync-schema-reference.mjs from ../flightcontrol2/packages/schemas/schema_reference/docs/project-config.md. Do not edit by hand. */}

The declarative project configuration file applied with `ravion projects config apply --file`. It contains environments, each with module instances.

## Legend

| Notation | Meaning |
| --- | --- |
| `T[]` | Array of `T` |
| `map<str,T>` | Object keyed by strings with `T` values |
| `enum[a,b]` | One of the listed values |
| `A \| B` | One of several types |
| ~template | Field accepts `<< ... >>` template expressions |

## ProjectConfig

User-authored project config file consumed by `ravion projects config apply --file`.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `project` | [Project](#project) | No | — | Project metadata to update before applying environment configuration. |
| `environments` | [Environment](#environment)[] | Yes | — | Environment configuration blocks to apply. |

## Project

Writable project metadata accepted by project config apply.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | No | — | Existing project ID. When provided, it must match the project in the route. |
| `givenId` | str | No | — | User-provided project identifier. When provided, it must match the project in the route. |
| `name` | str | No | — | — |
| `description` | str | No | — | — |

## Environment

Environment block authored in project config files.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | No | — | Existing environment ID. |
| `givenId` | str | No | — | User-provided environment identifier. |
| `name` | str | No | — | — |
| `description` | str | No | — | — |
| `moduleInstances` | [ModuleInstance](#moduleinstance)[] | Yes | — | — |

## ModuleInstance

Module instance shape authored in project config files.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | No | — | Existing module instance ID. Non-empty IDs must resolve to an existing module. |
| `givenId` | str | No | — | User-provided unique identifier. Must be unique within environment. |
| `name` | str | No | — | — |
| `description` | str | No | — | — |
| `type` | str | No | — | Module type identifier. |
| `version` | str | No | — | Semantic module version string. |
| `input` | map&lt;str,any \| null&gt; | No | — | Runtime module configuration input. |

## Module references

Use `{moduleGivenIdRef: "..."}` inside module input fields that reference another module.

| Scope | Reference |
| --- | --- |
| Same environment | `{moduleGivenIdRef: "module"}` |
| Cross environment, same project | `{moduleGivenIdRef: "environment.module"}` |
| Cross project, same organization | `{moduleGivenIdRef: "project.environment.module"}` |

Use `givenId` values for the project, environment, and module segments.
