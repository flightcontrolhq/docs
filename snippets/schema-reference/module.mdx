{/* Generated by scripts/sync-schema-reference.mjs from ../flightcontrol2/packages/schemas/schema_reference/docs/module.md. Do not edit by hand. */}

The module definition schema. A module declares its input form, dashboard UI, and stack, build, and deploy behavior.

## Legend

| Notation | Meaning |
| --- | --- |
| `T[]` | Array of `T` |
| `map<string,T>` | Object keyed by strings with `T` values |
| `enum[a,b]` | One of the listed values |
| `A \| B` | One of several types |
| ~template | Field accepts `<< ... >>` template expressions |

## Module

The root module schema.

- **`inputs`**: [InputProperty](#inputproperty)[] — required. Input configuration defining the module's form fields
- **`stack`**: [ModuleStack](#modulestack) — optional. Stack configuration for provisioning this module
- **`build`**: [ModuleBuild](#modulebuild) — optional. Build configuration for this module
- **`deploy`**: [ModuleDeployment](#moduledeployment) — optional. Deployment configuration for this module
- **`ui`**: [ModuleUI](#moduleui) — optional. UI configuration for links, logs, and metrics on the module dashboard
- **`readme`**: string — optional. Human-readable module documentation in Markdown.

## Inputs

Input configuration defining the module's form fields

### InputProperty

Union of all input property types.

One of: [StringInputProperty](#stringinputproperty), [TextInputProperty](#textinputproperty), [ObjectInputProperty](#objectinputproperty), [ObjectMapInputProperty](#objectmapinputproperty), [ObjectArrayInputProperty](#objectarrayinputproperty), [ArrayInputProperty](#arrayinputproperty), [ArrayStringInputProperty](#arraystringinputproperty), [NumberInputProperty](#numberinputproperty), [BooleanInputProperty](#booleaninputproperty), [SectionInputProperty](#sectioninputproperty), [CompoundInputProperty](#compoundinputproperty), [RefInputProperty](#refinputproperty), [GitRepoInputProperty](#gitrepoinputproperty), [KeyValueInputProperty](#keyvalueinputproperty).

### ArrayInputProperty

Array editor input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `array` — required. Input type for array editor fields
- **`default`**: any[] \| null — optional. Default array value for the field _(templateable)_
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in

### ArrayStringInputProperty

Array of strings input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `string_array` — required. Input type for dynamic string arrays
- **`default`**: string[] \| null — optional. Default array of values _(templateable)_
- **`placeholder`**: string — optional. Placeholder text for each input field in the array
- **`add_button_label`**: string — optional. Label for the "Add" button below the list
- **`values`**: [StringValues](#stringvalues) — optional. Allowed values — static array of options or dynamic reference for a multi-select dropdown
- **`required`**: boolean — optional. Whether at least one item is required
- **`patterns`**: [ValidationPattern](#validationpattern)[] — optional. Regex patterns to validate each array item against

### BooleanInputProperty

Boolean toggle/checkbox input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `boolean` — required. Input type for boolean toggle fields
- **`default`**: boolean — required. Default boolean value _(templateable)_

### CompoundInputProperty

Compound select input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `compound` — required. Input type for compound select dropdowns
- **`fields`**: string[] — required. List of field names to expose from the selected compound value _(minItems:1)_
- **`default`**: map&lt;string,string \| number \| integer \| boolean \| string[]&gt; \| "$values:first" — optional. Default value — a record with the field values _(templateable)_
- **`required`**: boolean — optional. Whether the field must be filled in
- **`values`**: [CompoundValues](#compoundvalues) — optional. Allowed values — static array of options or dynamic reference (required for compound)
- **`no_options_message`**: string — optional. Message shown when the dropdown has no available options
- **`min_current_value`**: boolean — optional. Use the current deployed value as the minimum allowed value

### GitRepoInputProperty

Git repository input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `gitrepo` — required. Input type for git repository selector
- **`default`**: string — optional. Default repository URL
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in

### KeyValueInputProperty

Key-value pairs input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `keyvalue` — required. Input type for key-value editor fields
- **`default`**: map&lt;string,string&gt; \| null — optional. Default key-value pairs for the field _(templateable)_
- **`required`**: boolean — optional. Whether the field must have at least one key-value pair

### NumberInputProperty

Numeric input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `number` — required. Input type for numeric fields
- **`default`**: integer \| "$values:first" — optional. Default numeric value _(templateable)_
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in
- **`values`**: [NumberValues](#numbervalues) — optional. Allowed values — static array of options or dynamic reference for a dropdown
- **`no_options_message`**: string — optional. Message shown when the dropdown has no available options
- **`min`**: integer — optional. Minimum allowed value _(format:int32)_
- **`max`**: integer — optional. Maximum allowed value _(format:int32)_
- **`min_current_value`**: boolean — optional. Use the current deployed value as the minimum allowed value

### ObjectArrayInputProperty

Structured array input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `object_array` — required. Input type for structured object arrays
- **`default`**: [ObjectArrayDefaultItem](#objectarraydefaultitem)[] \| null — optional. Default array value for the field _(templateable)_
- **`required`**: boolean — optional. Whether the field must contain at least one item
- **`item_label`**: string — optional. Label for each array item, used in add buttons and editor headers
- **`item_inputs`**: [RefFallbackInputProperty](#reffallbackinputproperty)[] — required. Input definitions for each object value in the array. _(minItems:1)_

### ObjectInputProperty

Object editor input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `object` — required. Input type for object editor fields
- **`default`**: any \| null — optional. Default object value for the field _(templateable)_
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in

### ObjectMapInputProperty

Structured map input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `object_map` — required. Input type for structured object maps
- **`default`**: map&lt;string,any \| null&gt; — optional. Default map value for the field _(templateable)_
- **`required`**: boolean — optional. Whether the field must contain at least one entry
- **`key`**: [MapKeyConfig](#mapkeyconfig) — optional. Metadata for the map entry key field.
- **`item_title`**: [ObjectMapItemTitleConfig](#objectmapitemtitleconfig) — optional. User-facing title template for each map item.
- **`item_label`**: string — optional. Label for each map item, used in add buttons and editor headers
- **`item_inputs`**: [RefFallbackInputProperty](#reffallbackinputproperty)[] — required. Input definitions for each object value in the map. _(minItems:1)_

### RefInputProperty

Module reference input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: string — required. Reference to another module type — format is "$ref:&lt;module-type&gt;"
- **`mapped_inputs`**: [RefFallbackInputProperty](#reffallbackinputproperty)[] — optional. Input definitions that copy values from the selected referenced module into this module's input values. Each mapped input is saved as a normal input on the c...
- **`required`**: boolean — optional. Whether the module reference is required

### SectionInputProperty

Visual section separator property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`type`**: `section` — required. Input type for visual section separators — produces no value

### StringInputProperty

Single-line text input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `string` — required. Input type for single-line text fields
- **`default`**: string \| "$values:first" — optional. Default value for the field _(templateable)_
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in
- **`values`**: [StringValues](#stringvalues) — optional. Allowed values — static array of options or dynamic reference for a dropdown
- **`no_options_message`**: string — optional. Message shown when the dropdown has no available options
- **`patterns`**: [ValidationPattern](#validationpattern)[] — optional. Regex patterns to validate against
- **`min_current_value`**: boolean — optional. Use the current deployed value as the minimum allowed value

### TextInputProperty

Multi-line text area input property.

- **`id`**: string — required. Unique identifier for this input. _(minLen:1)_
- **`label`**: string — required. Text shown above the form field _(minLen:1)_
- **`description`**: string — optional. Help text shown below the label
- **`show_when`**: [ShowWhen](#showwhen) — optional. Only show this field when another field has a specific value
- **`immutable`**: boolean — optional. Lock this field after first deploy — prevents changes to destructive settings
- **`collapsible`**: boolean — optional. Render this field as collapsible (tag when collapsed, full field when expanded) Supported for all value-producing input types.
- **`type`**: `text` — required. Input type for multi-line text areas
- **`default`**: string \| null — optional. Default value for the field _(templateable)_
- **`placeholder`**: string — optional. Placeholder text when the field is empty
- **`required`**: boolean — optional. Whether the field must be filled in
- **`patterns`**: [ValidationPattern](#validationpattern)[] — optional. Regex patterns to validate against

### ShowWhen

Conditional visibility mapping.

Type: map&lt;string,[ShowWhenCondition](#showwhencondition)&gt;

### StringValues

String values - either a static array of options or a dynamic reference.

One of: [StringValueItem](#stringvalueitem)[], string.

### ValidationPattern

A regex pattern with an error message for validation

- **`pattern`**: string — required. JavaScript regex the input must match
- **`message`**: string — required. Error shown to the user when validation fails

### CompoundFieldValue

Allowed value types within a compound value item.

One of: string, number, integer, boolean, string[].

### CompoundValues

Compound values - either a static array of options or a dynamic reference.

One of: [CompoundValueItem](#compoundvalueitem)[], string.

### DefaultValueRef

Special default references derived from the field's values list.

Allowed values:

- `$values:first`

### NumberValues

Number values - either a static array of options or a dynamic reference.

One of: [NumberValueItem](#numbervalueitem)[], string.

### ObjectArrayDefaultItem

Default value item for structured object arrays.

Type: map&lt;string,any \| null&gt;

### RefFallbackInputProperty

Input properties allowed inside module reference fallback inputs.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `array` | [ArrayInputProperty](#arrayinputproperty) |
| `boolean` | [BooleanInputProperty](#booleaninputproperty) |
| `compound` | [CompoundInputProperty](#compoundinputproperty) |
| `gitrepo` | [GitRepoInputProperty](#gitrepoinputproperty) |
| `keyvalue` | [KeyValueInputProperty](#keyvalueinputproperty) |
| `number` | [NumberInputProperty](#numberinputproperty) |
| `object` | [ObjectInputProperty](#objectinputproperty) |
| `object_array` | [ObjectArrayInputProperty](#objectarrayinputproperty) |
| `object_map` | [ObjectMapInputProperty](#objectmapinputproperty) |
| `section` | [SectionInputProperty](#sectioninputproperty) |
| `string` | [StringInputProperty](#stringinputproperty) |
| `string_array` | [ArrayStringInputProperty](#arraystringinputproperty) |
| `text` | [TextInputProperty](#textinputproperty) |

### MapKeyConfig

Configuration for the string key of an object_map entry.

- **`label`**: string — optional. Text shown for the map key field.
- **`placeholder`**: string — optional. Placeholder text for a new map key.
- **`description`**: string — optional. Help text shown below the map key field.
- **`patterns`**: [ValidationPattern](#validationpattern)[] — optional. Regex patterns to validate map keys against.
- **`from_fields`**: string[] — optional. Item input fields used to compute the map key instead of asking for one.
- **`separator`**: string — optional. String used to join computed key field values. Defaults to "-".

### ObjectMapItemTitleConfig

Configuration for the title shown for each object_map entry in the form.

- **`template`**: string — required. Template for the item title. Supports placeholders like &#123;key&#125; and &#123;field_id&#125;.
- **`fallback`**: string — optional. Fallback title template used when the primary template cannot be resolved.

### RefTypeDiscriminator

Type discriminator for module references.

Type: string

### ShowWhenCondition

A show_when condition.

One of: string \| integer \| boolean \| string \| integer \| boolean[], [ShowWhenNotCondition](#showwhennotcondition).

### DynamicValuesRef

Dynamic values reference.

Type: string

### StringValueItem

A selectable option with a string value.

- **`label`**: string — optional. Text shown in the dropdown
- **`description`**: string — optional. Extra detail shown alongside the option
- **`group_label`**: string — optional. Groups options under a shared heading in the dropdown
- **`value`**: string — required. Actual value sent to Terraform when this option is selected

### CompoundValueItem

A compound value item - a record with arbitrary fields plus select metadata.

- **`label`**: string — optional. Text shown in the dropdown
- **`description`**: string — optional. Extra detail shown alongside the option
- **`group_label`**: string — optional. Groups options under a shared heading in the dropdown

### NumberValueItem

A selectable option with a numeric value.

- **`label`**: string — optional. Text shown in the dropdown
- **`description`**: string — optional. Extra detail shown alongside the option
- **`group_label`**: string — optional. Groups options under a shared heading in the dropdown
- **`value`**: integer — required. Actual number sent to Terraform when this option is selected _(format:int32)_

### ShowWhenComparableValue

Value or values to compare against in a show_when condition.

One of: string \| integer \| boolean, string \| integer \| boolean[].

### ShowWhenNotCondition

Negated show_when condition.

- **`not`**: string \| integer \| boolean \| string \| integer \| boolean[] — required. Show the property only when the source value does not match this condition.

### ShowWhenValue

Value that can be used in a show_when condition.

One of: string, integer, boolean.

## Stack

Stack configuration for provisioning this module

### ModuleStack

Root stack block in module.yaml

- **`type`**: `terraform` | `opentofu` — required. IaC stack type _(templateable)_
- **`pipelines`**: [StackPipelinesConfig](#stackpipelinesconfig) — required. Configuration for all stack pipelines
- **`ravion_state_backend_workspace`**: string \| null — optional. The Ravion Cloud workspace name to use for state storage. _(templateable)_

### StackPipelinesConfig

Stack pipeline configuration

- **`defaults`**: [StackPipelineDefaults](#stackpipelinedefaults) — required. Defaults applied to stack runs and merged into trigger run inputs.
- **`change`**: [StackActionPipelineConfig](#stackactionpipelineconfig) — required. Configuration for stack change pipeline
- **`destroy`**: [StackActionPipelineConfig](#stackactionpipelineconfig) — required. Configuration for stack destroy pipeline

### StackActionPipelineConfig

Pipeline configuration for a stack action

- **`pipeline_id`**: string — required. Pipeline ID for this stack action _(minLen:1; templateable)_
- **`triggers`**: [Pipeline.PipelineTrigger](#pipeline-pipelinetrigger)[] — optional. Triggers that automatically start pipeline runs on webhook events.

### StackPipelineDefaults

Default stack pipeline run configuration

- **`variant`**: string — required. Default variant used for stack-triggered runs _(minLen:1; templateable)_
- **`input`**: map&lt;string,any \| null&gt; — optional. Default input values merged into each trigger run variant _(templateable)_

### Pipeline.PipelineTrigger

Pipeline trigger definition.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `webhook` | [Pipeline.GenericPipelineTrigger](#pipeline-genericpipelinetrigger) |
| `"webhook:github"` | [Pipeline.GithubPipelineTrigger](#pipeline-githubpipelinetrigger) |
| `"webhook:gitlab"` | [Pipeline.GitlabPipelineTrigger](#pipeline-gitlabpipelinetrigger) |

### Pipeline.GenericPipelineTrigger

Pipeline trigger for generic webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [Pipeline.TriggerFilter](#pipeline-triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[Pipeline.TriggerRunVariantConfig](#pipeline-triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook` — required. Webhook type for generic webhook triggers.

### Pipeline.GithubPipelineTrigger

Pipeline trigger for GitHub webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [Pipeline.TriggerFilter](#pipeline-triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[Pipeline.TriggerRunVariantConfig](#pipeline-triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook:github` — required. Webhook type — determines which events are supported.
- **`event`**: `push` | `pull_request` | `pull_request_review` | `pull_request_review_comment` | `pull_request_review_thread` | `release` | `deployment` — required. Webhook event type to listen for.

### Pipeline.GitlabPipelineTrigger

Pipeline trigger for GitLab webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [Pipeline.TriggerFilter](#pipeline-triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[Pipeline.TriggerRunVariantConfig](#pipeline-triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook:gitlab` — required. Webhook type — determines which events are supported.
- **`event`**: `push` | `merge_request` | `tag_push` — required. Webhook event type to listen for.

### Pipeline.TriggerFilter

Trigger filter configuration

- **`branch`**: string \| null — optional. Only trigger when this branch is pushed _(templateable)_
- **`tag`**: string \| null — optional. Only trigger when a tag matching this pattern is pushed _(templateable)_
- **`action`**: string \| string[] — optional. Only trigger for specific webhook actions. _(templateable)_
- **`paths`**: string[] \| null — optional. Only trigger when files in these paths are changed _(templateable)_
- **`payload`**: [Pipeline.PayloadCondition](#pipeline-payloadcondition)[] — optional. Filter by webhook payload fields using dot-notation paths.

### Pipeline.TriggerRunVariantConfig

Per-variant run configuration for a trigger.

- **`description`**: string \| null — optional. Description for the pipeline run created by this trigger variant _(templateable)_
- **`input`**: map&lt;string,any \| null&gt; — optional. Input overrides for this variant run _(templateable)_

### Pipeline.GithubWebhookEvent

GitHub webhook events that can trigger a pipeline

Allowed values:

- `push`
- `pull_request`
- `pull_request_review`
- `pull_request_review_comment`
- `pull_request_review_thread`
- `release`
- `deployment`

### Pipeline.GitlabWebhookEvent

GitLab webhook events that can trigger a pipeline

Allowed values:

- `push`
- `merge_request`
- `tag_push`

### Pipeline.PayloadCondition

Condition to check against a webhook payload field.

- **`path`**: string — required. Dot-notation path to the payload field
- **`equals`**: string \| null — optional. Exact string match _(templateable)_
- **`contains`**: string \| null — optional. Substring match _(templateable)_
- **`matches`**: string \| null — optional. Glob pattern match _(templateable)_
- **`one_of`**: string[] \| null — optional. Value must be one of these strings _(templateable)_
- **`not`**: [Pipeline.PayloadConditionMatcher](#pipeline-payloadconditionmatcher) — optional. Negated payload field matcher. The condition fails when this matcher is true.

### Pipeline.PayloadConditionMatcher

Payload field matcher used by positive conditions and negated conditions.

- **`equals`**: string \| null — optional. Exact string match _(templateable)_
- **`contains`**: string \| null — optional. Substring match _(templateable)_
- **`matches`**: string \| null — optional. Glob pattern match _(templateable)_
- **`one_of`**: string[] \| null — optional. Value must be one of these strings _(templateable)_

## Build

Build configuration for this module

### ModuleBuild

Build configuration — discriminated by module build type

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `disabled` | [DisabledModuleBuild](#disabledmodulebuild) |
| `image` | [ImageModuleBuild](#imagemodulebuild) |
| `static` | [StaticModuleBuild](#staticmodulebuild) |

### DisabledModuleBuild

Disabled build configuration for a module. Additional fields are ignored.

- **`type`**: `disabled` — required. Build type discriminator

### ImageModuleBuild

Image build configuration for a module.

- **`type`**: `image` — required. Build type discriminator _(templateable)_
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the build is killed. _(format:int32,min:1; templateable)_
- **`inputs`**: [InputProperty](#inputproperty)[] — required. Build-specific inputs shown when running a module build.
- **`source`**: [Pipeline.GitSource](#pipeline-gitsource) — required. Git source configuration. _(templateable)_
- **`builder`**: [Pipeline.ImageBuildConfig](#pipeline-imagebuildconfig) — required. Builder configuration — dockerfile or nixpacks settings. _(templateable)_
- **`environment_variables`**: [Pipeline.EnvironmentVariablesValue](#pipeline-environmentvariablesvalue) — optional. Environment variables available during the build. _(templateable)_
- **`debug`**: boolean \| null — optional. Enable verbose build output for debugging build issues. _(templateable)_
- **`destinations`**: [Pipeline.EcrDestination](#pipeline-ecrdestination)[] — required. ECR destinations where the built Docker image will be pushed. _(minItems:1; templateable)_
- **`infrastructure`**: [Pipeline.StepInfrastructure](#pipeline-stepinfrastructure) — required. Execution environment configuration used for the build. _(templateable)_

### StaticModuleBuild

Static build configuration for a module.

- **`type`**: `static` — required. Build type discriminator _(templateable)_
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the build is killed. _(format:int32,min:1; templateable)_
- **`inputs`**: [InputProperty](#inputproperty)[] — required. Build-specific inputs shown when running a module build.
- **`source`**: [Pipeline.GitSource](#pipeline-gitsource) — required. Git source configuration. _(templateable)_
- **`builder`**: [Pipeline.StaticBuildConfig](#pipeline-staticbuildconfig) — required. Builder configuration — dockerfile or nixpacks settings. _(templateable)_
- **`environment_variables`**: [Pipeline.EnvironmentVariablesValue](#pipeline-environmentvariablesvalue) — optional. Environment variables available during the build. _(templateable)_
- **`debug`**: boolean \| null — optional. Enable verbose build output for debugging build issues. _(templateable)_
- **`destinations`**: [Pipeline.S3StaticDestination](#pipeline-s3staticdestination)[] — required. S3 destinations where the built static assets will be uploaded. _(minItems:1; templateable)_
- **`infrastructure`**: [Pipeline.StepInfrastructure](#pipeline-stepinfrastructure) — required. Execution environment configuration used for the build. _(templateable)_

### Pipeline.EcrDestination

ECR destination for image builds

- **`id`**: string — required. Unique destination identifier within the build step _(templateable)_
- **`type`**: `ecr` — required. ECR destination type _(templateable)_
- **`repository_arn`**: string — required. ECR repository ARN _(templateable)_
- **`tags`**: string[] \| null — optional. Docker image tags to apply in ECR after the build finishes _(templateable)_

### Pipeline.EnvironmentVariablesValue

Environment variable map value.

One of: map&lt;string,[Pipeline.EnvironmentVariable](#pipeline-environmentvariable)&gt;, string.

### Pipeline.GitSource

Git source configuration for fetching code

- **`type`**: `git` — required. Source type identifier _(templateable)_
- **`repo`**: string — required. Full repository URL including host _(templateable)_
- **`branch`**: string — required. Branch to check out _(minLen:1; templateable)_
- **`ref`**: string \| null — optional. Git ref to check out — branch, commit SHA, tag, or other ref. Overrides branch HEAD if set. _(templateable)_
- **`base_path`**: string \| null — optional, default `.`. Base path within the repository to use as the working root _(templateable)_

### Pipeline.ImageBuildConfig

Image build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `disabled` | [Pipeline.DisabledBuildConfig](#pipeline-disabledbuildconfig) |
| `dockerfile` | [Pipeline.ImageDockerfileBuildConfig](#pipeline-imagedockerfilebuildconfig) |
| `nixpacks` | [Pipeline.ImageNixpacksBuildConfig](#pipeline-imagenixpacksbuildconfig) |

### Pipeline.StepInfrastructure

Step infrastructure configuration

- **`execution_environment_id`**: string \| null — optional. ID or given ID of a pre-configured execution environment _(templateable)_
- **`aws_account_id`**: string \| null — optional. AWS account reference for automatic network selection. _(templateable)_
- **`region`**: string \| null — optional. AWS region for automatic network selection _(templateable)_
- **`type`**: `ec2` | `ec2-spot` — required. Instance type — "ec2" for on-demand or "ec2-spot" for cheaper spot instances _(templateable)_
- **`instance_size`**: string \| string[] — required. Instance size — a single size like "large" or an array like ["large", "xlarge"] for spot fallback _(templateable)_
- **`ami`**: string \| null — optional. AMI ID for the runner instance _(templateable)_
- **`permissions`**: [Pipeline.AwsPermissions](#pipeline-awspermissions) — optional. IAM permissions for the runner role — "attach" to add policies or "replace" to override step defaults _(templateable)_
- **`storage`**: [Pipeline.StorageConfig](#pipeline-storageconfig) — optional. Root volume storage configuration _(templateable)_

### Pipeline.S3StaticDestination

S3 destination for static builds

- **`id`**: string — required. Unique destination identifier within the build step _(minLen:1; templateable)_
- **`type`**: `s3` — required. S3 destination type _(templateable)_
- **`bucket`**: string — required. S3 bucket name where built assets will be uploaded _(templateable)_
- **`directory`**: string \| null — optional. S3 directory override for the uploaded build output. _(templateable)_
- **`region`**: string — required. AWS region for the destination S3 bucket _(templateable)_

### Pipeline.StaticBuildConfig

Static build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `dockerfile` | [Pipeline.StaticDockerfileBuildConfig](#pipeline-staticdockerfilebuildconfig) |
| `nixpacks` | [Pipeline.StaticNixpacksBuildConfig](#pipeline-staticnixpacksbuildconfig) |

### Pipeline.EnvironmentVariable

Environment variable — can be a plain string, Parameter Store reference, or Secrets Manager reference

One of: string \| null, [Pipeline.ParameterStoreEnvVar](#pipeline-parameterstoreenvvar), [Pipeline.SecretsManagerEnvVar](#pipeline-secretsmanagerenvvar).

### Pipeline.GitRepoUrl

Full git repository URL including host.

Type: string

### Pipeline.DisabledBuildConfig

Disabled build configuration. Valid only for module-driven builds.

- **`type`**: `disabled` — required. Build method — disables module build execution _(templateable)_

### Pipeline.ImageDockerfileBuildConfig

Dockerfile configuration for image builds

- **`type`**: `dockerfile` — required. Build method — use a custom Dockerfile _(templateable)_
- **`dockerfile`**: string \| null — optional, default `Dockerfile`. Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_
- **`context`**: string \| null — optional, default `.`. Docker build context path relative to source.base_path _(templateable)_
- **`inject_env_variables_in_dockerfile`**: boolean \| null — optional. Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_
- **`cache_from`**: [Pipeline.CacheFromConfig](#pipeline-cachefromconfig) — optional. Docker layer cache configuration _(templateable)_

### Pipeline.ImageNixpacksBuildConfig

Nixpacks configuration for image builds

- **`type`**: `nixpacks` — required. Build method — use Nixpacks auto-detection _(templateable)_
- **`nixpacks_version`**: string \| null — optional. Nixpacks version to use _(templateable)_
- **`build_path`**: string \| null — optional. Directory to run the Nixpacks build from, relative to the repository root _(templateable)_
- **`config_file_path`**: string \| null — optional. Path to the Nixpacks configuration file relative to the repository root _(templateable)_
- **`nix_pkgs`**: string[] \| null — optional. Additional Nix packages to install _(templateable)_
- **`apt_pkgs`**: string[] \| null — optional. Additional apt packages to install _(templateable)_
- **`nix_libs`**: string[] \| null — optional. Additional Nix libraries to install _(templateable)_
- **`install_cmd`**: string \| null — optional. Custom install command _(templateable)_
- **`build_cmd`**: string \| null — optional. Custom build command _(templateable)_
- **`start_cmd`**: string \| null — optional. Application start command _(templateable)_
- **`cache_from`**: [Pipeline.CacheFromConfig](#pipeline-cachefromconfig) — optional. Docker layer cache configuration _(templateable)_

### Pipeline.AwsPermissions

IAM policies configuration for runner roles Used by EC2-backed steps to specify additional permissions

One of: [Pipeline.AwsPermissionsAttach](#pipeline-awspermissionsattach), [Pipeline.AwsPermissionsReplace](#pipeline-awspermissionsreplace).

### Pipeline.InfrastructureType

Execution environment type

Allowed values:

- `ec2`
- `ec2-spot`

### Pipeline.InstanceSize

Instance size can be a single string or array of strings

One of: string, string[].

### Pipeline.StorageConfig

Root volume storage configuration

- **`type`**: `gp3` | `gp2` | `io2` | `io1` — optional. Volume type _(templateable)_
- **`size`**: integer \| null — optional. Volume size in GiB _(format:int32; templateable)_
- **`iops`**: integer \| null — optional. Provisioned IOPS _(format:int32; templateable)_
- **`throughput`**: integer \| null — optional. Provisioned throughput in MiB/s _(format:int32; templateable)_

### Pipeline.StaticDockerfileBuildConfig

Dockerfile configuration for static builds

- **`type`**: `dockerfile` — required. Build method — use a custom Dockerfile _(templateable)_
- **`dockerfile`**: string \| null — optional, default `Dockerfile`. Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_
- **`context`**: string \| null — optional, default `.`. Docker build context path relative to source.base_path _(templateable)_
- **`inject_env_variables_in_dockerfile`**: boolean \| null — optional. Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_
- **`output_directory`**: string — required. Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_

### Pipeline.StaticNixpacksBuildConfig

Nixpacks configuration for static builds

- **`type`**: `nixpacks` — required. Build method — use Nixpacks auto-detection _(templateable)_
- **`nixpacks_version`**: string \| null — optional. Nixpacks version to use _(templateable)_
- **`build_path`**: string \| null — optional. Directory to run the Nixpacks build from, relative to the repository root _(templateable)_
- **`config_file_path`**: string \| null — optional. Path to the Nixpacks configuration file relative to the repository root _(templateable)_
- **`nix_pkgs`**: string[] \| null — optional. Additional Nix packages to install _(templateable)_
- **`apt_pkgs`**: string[] \| null — optional. Additional apt packages to install _(templateable)_
- **`nix_libs`**: string[] \| null — optional. Additional Nix libraries to install _(templateable)_
- **`install_cmd`**: string \| null — optional. Custom install command _(templateable)_
- **`build_cmd`**: string \| null — optional. Custom build command _(templateable)_
- **`output_directory`**: string — required. Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_

### Pipeline.ParameterStoreEnvVar

Environment variable from AWS Parameter Store

- **`from_parameter_store`**: [Pipeline.ParameterStoreReferenceValue](#pipeline-parameterstorereferencevalue) — required. Parameter Store reference _(templateable)_

### Pipeline.SecretsManagerEnvVar

Environment variable from AWS Secrets Manager

- **`from_secrets_manager`**: [Pipeline.SecretsManagerReferenceValue](#pipeline-secretsmanagerreferencevalue) — required. Secrets Manager reference _(templateable)_

### Pipeline.CacheFromConfig

Configuration for Docker layer cache source from ECR

- **`digest`**: string \| null — optional. Specific image digest to use for cache _(templateable)_
- **`tag`**: string \| null — optional. Specific image tag to use for cache _(templateable)_

### Pipeline.AwsPermissionsAttach

IAM policies to attach alongside step default policies

- **`attach`**: string[] — required. AWS managed policy ARNs to attach alongside step default policies _(templateable)_

### Pipeline.AwsPermissionsReplace

IAM policies to replace step default policies entirely

- **`replace`**: string[] — required. AWS managed policy ARNs to use instead of step default policies _(templateable)_

### Pipeline.EbsVolumeType

EBS volume type for the runner root volume

Allowed values:

- `gp3`
- `gp2`
- `io2`
- `io1`

### Pipeline.ParameterStoreReferenceValue

Parameter Store reference — shorthand string or object with optional version metadata

One of: string, [Pipeline.ParameterStoreReference](#pipeline-parameterstorereference).

### Pipeline.SecretsManagerReferenceValue

Secrets Manager reference — shorthand string or object with optional JSON key/version metadata

One of: string, [Pipeline.SecretsManagerReference](#pipeline-secretsmanagerreference).

### Pipeline.ParameterStoreReference

Parameter Store reference with optional version metadata

- **`key`**: string — required. Parameter Store parameter name or path _(templateable)_
- **`version`**: string \| null — optional. Parameter version number _(templateable)_

### Pipeline.SecretsManagerReference

Secrets Manager reference with optional JSON key and version metadata

- **`key`**: string — required. Secret name or ARN in Secrets Manager _(templateable)_
- **`json_key`**: string \| null — optional. JSON key to extract from a JSON-structured secret _(templateable)_
- **`version`**: string \| null — optional. Secret version ID or stage _(templateable)_

## Deploy

Deployment configuration for this module

### ModuleDeployment

Deployment configuration — discriminated by deployment type

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `"aws:ecs"` | [EcsModuleDeployment](#ecsmoduledeployment) |
| `"aws:static"` | [AwsStaticModuleDeployment](#awsstaticmoduledeployment) |

### AwsStaticModuleDeployment

AWS static site deployment configuration

- **`type`**: `aws:static` — required. Deployment type discriminator
- **`infrastructure`**: [AwsStaticDeploymentInfrastructure](#awsstaticdeploymentinfrastructure) — required. Infrastructure resources required by the deployment
- **`timeout`**: integer — optional. Maximum total wall-clock budget (in seconds) for a single deploy, measured from the moment the workflow starts (including time spent waiting for the deployme... _(format:int32,min:60)_
- **`inputs`**: [InputProperty](#inputproperty)[] — required. Deployment-specific inputs shown in the deploy modal.
- **`definition`**: [AwsStaticDeploymentDefinition](#awsstaticdeploymentdefinition) — required. Per-deploy definition. Templateable — supports both module.input and deploy.input references inside string fields. _(templateable)_

### EcsModuleDeployment

AWS ECS deployment configuration

- **`type`**: `aws:ecs` — required. Deployment type discriminator
- **`infrastructure`**: [EcsDeploymentInfrastructure](#ecsdeploymentinfrastructure) — required. Infrastructure resources required by the deployment
- **`concurrency`**: [EcsDeployConcurrency](#ecsdeployconcurrency) — optional. Concurrency policy — controls how simultaneous deploy requests are handled.
- **`timeout`**: integer — optional. Maximum total wall-clock budget (in seconds) for a single deploy, measured from the moment the workflow starts (including time spent waiting for the deployme... _(format:int32,min:60)_
- **`inputs`**: [InputProperty](#inputproperty)[] — required. Deployment-specific inputs shown in the deploy modal Same types as module inputs — reuses the InputProperty union
- **`task_definition`**: [EcsTaskDefinition](#ecstaskdefinition) — required. AWS ECS task definition — fully typed per the RegisterTaskDefinition API Templateable — supports both module.input and deploy.input references _(templateable)_
- **`pre_deploy`**: [EcsRunTaskOverrides](#ecsruntaskoverrides) — optional _(templateable)_
- **`post_deploy`**: [EcsRunTaskOverrides](#ecsruntaskoverrides) — optional _(templateable)_

### AwsStaticDeploymentDefinition

Per-deploy definition for AWS static deployments. The deploy manager promotes the resolved `s3_directory` to the `active` KVS key and (optionally) issues a b...

- **`s3_directory`**: string — required. S3 directory (version prefix) to promote to active. _(minLen:1; templateable)_
- **`cloudfront_invalidation_paths`**: string[] \| null — optional. CloudFront cache invalidation paths to issue across every distribution after the KVS write succeeds. Runs in the background — the deploy is marked complete o... _(templateable)_
- **`keep_latest_successful_deploy_count`**: integer \| null — optional. Number of latest successful deploys to keep in S3. Older S3 directories are pruned in the background after a successful deploy. Omit or set to 0 to disable p... _(format:int32,min:0; templateable)_

### AwsStaticDeploymentInfrastructure

Infrastructure resources required for AWS static site deployment.

- **`cloudfront_keyvaluestore_arn`**: string — required. CloudFront KeyValueStore ARN that holds the active version pointer. _(minLen:1; templateable)_
- **`cloudfront_distribution_arns`**: string[] — required. CloudFront distribution ARNs that share this origin/KVS. _(minItems:1; templateable)_
- **`s3_region`**: string — required. AWS region of the S3 hosting bucket. _(minLen:1; templateable)_
- **`s3_bucket`**: string — required. S3 bucket name that hosts the versioned build directories. _(minLen:1; templateable)_

### EcsDeployConcurrency

Concurrency policy for ECS deployments targeting this module instance.

- **`queue_size`**: integer — optional. Number of pending deploy requests that can wait in the queue. _(format:int32,min:0)_
- **`queue_overflow`**: `oldest` | `newest` | `reject` — optional. What to do when the queue is full and a new deploy request arrives.

### EcsDeploymentInfrastructure

Infrastructure resources required for ECS deployment. Not provisioned by the deploy manager — must already exist.

- **`ecs_cluster_arn`**: string — required. ECS cluster ARN where the service runs _(minLen:1; templateable)_
- **`ecs_service_arns`**: string[] — required. ECS service ARNs — 1 for rolling deploy, 2 for blue/green. _(minItems:1; templateable)_

### EcsRunTaskOverrides

Full AWS ECS RunTask request payload — both the `TaskOverride` fields and the top-level RunTask parameters that aren't part of `TaskOverride` (launchType, ne...

- **`container_overrides`**: [EcsContainerOverride](#ecscontaineroverride)[] \| null — optional. Per-container overrides. Each entry's `name` matches a container in the task definition. AWS RunTask supports overriding any subset of containers; the array... _(templateable)_
- **`cpu`**: string \| null — optional. Task-level CPU override (string for AWS compatibility — same format as task-definition `cpu`, e.g. `"256"`, `"1024"`). _(templateable)_
- **`memory`**: string \| null — optional. Task-level memory override (MiB, string format). _(templateable)_
- **`task_role_arn`**: string \| null — optional. IAM task role ARN override — what the container assumes when calling AWS APIs from inside the task. _(templateable)_
- **`execution_role_arn`**: string \| null — optional. IAM execution role ARN override — what ECS uses to pull the image, push logs, and read secrets at task launch. _(templateable)_
- **`ephemeral_storage`**: [EcsEphemeralStorage](#ecsephemeralstorage) — optional. Fargate ephemeral storage size override (20–200 GiB). _(templateable)_
- **`launch_type`**: `FARGATE` | `EC2` | `EXTERNAL` | `null` — optional. Launch type: FARGATE, EC2, or EXTERNAL. When omitted the hook inherits the parent service's launch type. Mutually exclusive with `capacity_provider_strategy`... _(templateable)_
- **`capacity_provider_strategy`**: [EcsCapacityProviderStrategyItem](#ecscapacityproviderstrategyitem)[] \| null — optional. Capacity-provider strategy — overrides `launch_type` when set. _(templateable)_
- **`platform_version`**: string \| null — optional. Fargate platform version (e.g. `"1.4.0"`, `"LATEST"`). _(templateable)_
- **`network_configuration`**: [EcsNetworkConfiguration](#ecsnetworkconfiguration) — optional. VPC / subnet / security-group config. Required for `awsvpc` network-mode task definitions; inherited from the parent service when omitted. _(templateable)_
- **`placement_constraints`**: [EcsPlacementConstraint](#ecsplacementconstraint)[] \| null — optional. Placement constraints — limits which container instances can run the task. EC2/EXTERNAL launch types only. _(templateable)_
- **`placement_strategy`**: [EcsPlacementStrategy](#ecsplacementstrategy)[] \| null — optional. Placement strategy — how tasks get distributed across instances. _(templateable)_
- **`propagate_tags`**: `TASK_DEFINITION` | `SERVICE` | `NONE` | `null` — optional. How tags propagate from task definition / service onto the task. _(templateable)_
- **`tags`**: [EcsTag](#ecstag)[] \| null — optional. Metadata tags applied to the task. _(templateable)_
- **`enable_ecs_managed_tags`**: boolean \| null — optional. Whether to enable AWS-managed `aws:ecs:` tags on the task. _(templateable)_
- **`enable_execute_command`**: boolean \| null — optional. Whether to enable ECS Exec on the task (for `aws ecs execute-command`). _(templateable)_
- **`group`**: string \| null — optional. Logical group name — used for placement constraints (`memberOf(group:&lt;group&gt;)`). Defaults to `family:&lt;task-def-family&gt;` when AWS picks. _(templateable)_
- **`reference_id`**: string \| null — optional. Caller-supplied reference id (UUID-shaped). Echoed back on the resulting Task and useful for client-side correlation. _(templateable)_
- **`volume_configurations`**: [EcsTaskVolumeConfiguration](#ecstaskvolumeconfiguration)[] \| null — optional. Per-task volume bindings — name must match a volume in the task definition. Used for Fargate-managed ephemeral EBS volumes. _(templateable)_
- **`timeout`**: integer \| null — optional. Wall-clock timeout in seconds before the task is stopped and the deployment fails. Only meaningful when used as a pre/post deploy hook config — ignored on th... _(format:int32,min:1; templateable)_

### EcsTaskDefinition

AWS ECS task definition — models the RegisterTaskDefinition API input.

- **`family`**: string — required. Task definition family name _(templateable)_
- **`container_definitions`**: [EcsContainerDefinition](#ecscontainerdefinition)[] — required. Container definitions — at least one required _(minItems:1; templateable)_
- **`cpu`**: string \| null — optional. Task-level CPU units (required for Fargate) Valid values: "256", "512", "1024", "2048", "4096" etc. _(templateable)_
- **`memory`**: string \| null — optional. Task-level memory in MiB (required for Fargate) _(templateable)_
- **`network_mode`**: `awsvpc` | `bridge` | `host` | `none` | `null` — optional. Network mode — "awsvpc" required for Fargate _(templateable)_
- **`requires_compatibilities`**: enum[FARGATE,EC2][] \| null — optional. Launch type compatibility _(templateable)_
- **`task_role_arn`**: string \| null — optional. IAM role ARN for task permissions (containers assume this role) _(templateable)_
- **`execution_role_arn`**: string \| null — optional. IAM role ARN for ECS agent to pull images and write logs _(templateable)_
- **`runtime_platform`**: [EcsRuntimePlatform](#ecsruntimeplatform) — optional. Runtime platform (Fargate only) _(templateable)_
- **`ephemeral_storage`**: [EcsEphemeralStorage](#ecsephemeralstorage) — optional. Ephemeral storage (Fargate only, &gt;20 GiB) _(templateable)_
- **`volumes`**: [EcsVolume](#ecsvolume)[] \| null — optional. Volumes available to containers _(templateable)_
- **`tags`**: [EcsTag](#ecstag)[] \| null — optional. AWS resource tags _(templateable)_
- **`ipc_mode`**: `host` | `task` | `none` | `null` — optional. IPC mode for containers (EC2 only) _(templateable)_
- **`pid_mode`**: `host` | `task` | `null` — optional. PID mode for containers (EC2 only) _(templateable)_
- **`proxy_configuration`**: [EcsProxyConfiguration](#ecsproxyconfiguration) — optional. App Mesh proxy configuration _(templateable)_
- **`inference_accelerators`**: [EcsInferenceAccelerator](#ecsinferenceaccelerator)[] \| null — optional. Inference accelerators (EC2 only) _(templateable)_

### EcsCapacityProviderStrategyItem

Capacity-provider strategy entry. Mirrors AWS exactly.

- **`capacity_provider`**: string — required. Capacity provider name (e.g. `FARGATE`, `FARGATE_SPOT`, or a custom EC2 capacity provider). _(templateable)_
- **`weight`**: integer \| null — optional. Relative share of tasks placed onto this provider when the strategy has multiple entries. _(format:int32,min:0,max:1000; templateable)_
- **`base`**: integer \| null — optional. Minimum number of tasks placed onto this provider before weighting kicks in. At most one entry in the strategy may set `base`. _(format:int32,min:0,max:100000; templateable)_

### EcsContainerOverride

Per-container portion of `EcsRunTaskOverrides`. Mirrors the AWS ECS RunTask `ContainerOverride` struct field-for-field — every override AWS supports for a co...

- **`name`**: string — required. Container name from the task definition. Required. _(minLen:1; templateable)_
- **`command`**: string[] \| null — optional. Replacement command — replaces the container's CMD array. _(templateable)_
- **`environment`**: [EcsEnvironmentVariable](#ecsenvironmentvariable)[] \| null — optional. Environment variables merged on top of the task definition's. _(templateable)_
- **`environment_files`**: [EcsEnvironmentFile](#ecsenvironmentfile)[] \| null — optional. Environment files (S3 references) layered on top of `environment`. _(templateable)_
- **`cpu`**: integer \| null — optional. CPU units override for this container. _(format:int32; templateable)_
- **`memory`**: integer \| null — optional. Hard memory limit (MiB) override for this container. _(format:int32; templateable)_
- **`memory_reservation`**: integer \| null — optional. Soft memory reservation (MiB) override for this container. _(format:int32; templateable)_
- **`resource_requirements`**: [EcsResourceRequirement](#ecsresourcerequirement)[] \| null — optional. GPU / InferenceAccelerator requirements override. _(templateable)_

### EcsEphemeralStorage

ECS ephemeral storage configuration

- **`size_in_gib`**: integer — required. Storage size in GiB (21–200 for Fargate, minimum 20) _(format:int32,min:20,max:200; templateable)_

### EcsNetworkConfiguration

AWS ECS network configuration. Only the `awsvpc` shape exists on RunTask (host / bridge networking is task-definition-driven).

- **`awsvpc_configuration`**: [EcsAwsVpcConfiguration](#ecsawsvpcconfiguration) — required. VPC configuration — required when set. _(templateable)_

### EcsPlacementConstraint

AWS ECS placement constraint. EC2/EXTERNAL launch types only — Fargate ignores these.

- **`type`**: `distinctInstance` | `memberOf` — required. Constraint type. `distinctInstance` spreads tasks one-per-host; `memberOf` filters by a cluster query expression. _(templateable)_
- **`expression`**: string \| null — optional. Cluster query expression — required when `type` is `memberOf`. _(templateable)_

### EcsPlacementStrategy

AWS ECS placement strategy. EC2/EXTERNAL launch types only — Fargate ignores these.

- **`type`**: `random` | `spread` | `binpack` — required. Strategy type. `random` ignores `field`; `spread` and `binpack` require `field`. _(templateable)_
- **`field`**: string \| null — optional. Attribute the strategy operates on (e.g. `instanceId`, `attribute:ecs.availability-zone`, `cpu`, `memory`). _(templateable)_

### EcsTag

AWS resource tag

- **`key`**: string — required. Tag key _(templateable)_
- **`value`**: string — required. Tag value _(templateable)_

### EcsTaskVolumeConfiguration

Per-task volume binding for ECS-managed volumes (today: ephemeral EBS for Fargate). The `name` must match a volume entry in the task definition's `volumes` l...

- **`name`**: string — required. Volume name — must match a `name` in the task definition. _(templateable)_
- **`managed_ebs_volume`**: [EcsTaskManagedEBSVolumeConfiguration](#ecstaskmanagedebsvolumeconfiguration) — optional. Managed-EBS volume configuration. _(templateable)_

### EcsContainerDefinition

ECS container definition — models a single container within a task.

- **`name`**: string — required. Container name — must be unique within the task _(templateable)_
- **`image`**: string — required. Docker image URI _(templateable)_
- **`cpu`**: integer \| null — optional. Container-level CPU units _(format:int32; templateable)_
- **`memory`**: integer \| null — optional. Hard memory limit in MiB — container is killed if it exceeds this _(format:int32; templateable)_
- **`memory_reservation`**: integer \| null — optional. Soft memory limit in MiB — used for memory reservation _(format:int32; templateable)_
- **`essential`**: boolean \| null — optional. Whether the task should stop if this container exits _(templateable)_
- **`port_mappings`**: [EcsPortMapping](#ecsportmapping)[] \| null — optional. Port mappings _(templateable)_
- **`environment`**: [EcsEnvironmentVariable](#ecsenvironmentvariable)[] \| null — optional. Environment variables _(templateable)_
- **`secrets`**: [EcsSecret](#ecssecret)[] \| null — optional. Secrets from Parameter Store or Secrets Manager _(templateable)_
- **`command`**: string[] \| null — optional. Startup command — overrides the Docker CMD _(templateable)_
- **`entry_point`**: string[] \| null — optional. Entrypoint — overrides the Docker ENTRYPOINT _(templateable)_
- **`working_directory`**: string \| null — optional. Working directory inside the container _(templateable)_
- **`health_check`**: [EcsHealthCheck](#ecshealthcheck) — optional. Container health check _(templateable)_
- **`log_configuration`**: [EcsLogConfiguration](#ecslogconfiguration) — optional. Log configuration _(templateable)_
- **`linux_parameters`**: [EcsLinuxParameters](#ecslinuxparameters) — optional. Linux-specific parameters _(templateable)_
- **`docker_labels`**: map&lt;string,string&gt; \| null — optional. Docker labels _(templateable)_
- **`privileged`**: boolean \| null — optional. Run as privileged (EC2 only) _(templateable)_
- **`user`**: string \| null — optional. User to run as (uid or uid:gid) _(templateable)_
- **`stop_timeout`**: integer \| null — optional. Stop timeout in seconds before SIGKILL _(format:int32; templateable)_
- **`disable_networking`**: boolean \| null — optional. Disable networking for this container _(templateable)_
- **`resource_requirements`**: [EcsResourceRequirement](#ecsresourcerequirement)[] \| null — optional. GPU and inference accelerator requirements _(templateable)_
- **`mount_points`**: [EcsMountPoint](#ecsmountpoint)[] \| null — optional. Volume mount points _(templateable)_
- **`volumes_from`**: [EcsVolumeFrom](#ecsvolumefrom)[] \| null — optional. Volumes to inherit from other containers _(templateable)_
- **`depends_on`**: [EcsContainerDependency](#ecscontainerdependency)[] \| null — optional. Container startup dependencies _(templateable)_
- **`repository_credentials`**: [EcsRepositoryCredentials](#ecsrepositorycredentials) — optional. Private registry credentials _(templateable)_
- **`firelens_configuration`**: [EcsFirelensConfiguration](#ecsfirelensconfiguration) — optional. Firelens log routing configuration _(templateable)_
- **`system_controls`**: [EcsSystemControl](#ecssystemcontrol)[] \| null — optional. System controls — kernel parameter overrides _(templateable)_
- **`environment_files`**: [EcsEnvironmentFile](#ecsenvironmentfile)[] \| null — optional. Files containing environment variables _(templateable)_
- **`readonly_root_filesystem`**: boolean \| null — optional. Read-only root filesystem _(templateable)_
- **`interactive`**: boolean \| null — optional. Interactive mode (stdin) _(templateable)_
- **`pseudo_terminal`**: boolean \| null — optional. Pseudo-terminal allocation _(templateable)_

### EcsInferenceAccelerator

Inference accelerator for ML workloads

- **`device_name`**: string — required. Accelerator name _(templateable)_
- **`device_type`**: string — required. Accelerator type _(templateable)_

### EcsProxyConfiguration

App Mesh proxy configuration

- **`type`**: `APPMESH` | `null` — optional. Proxy type _(templateable)_
- **`container_name`**: string — required. Container name for the proxy _(templateable)_
- **`properties`**: [EcsProxyConfigurationProperty](#ecsproxyconfigurationproperty)[] \| null — optional. Proxy configuration properties _(templateable)_

### EcsRuntimePlatform

ECS runtime platform configuration

- **`cpu_architecture`**: `X86_64` | `ARM64` | `null` — optional. CPU architecture — X86_64 or ARM64 _(templateable)_
- **`operating_system_family`**: `LINUX` | `WINDOWS_SERVER_2019_FULL` | `WINDOWS_SERVER_2019_CORE` | `WINDOWS_SERVER_2022_FULL` | `WINDOWS_SERVER_2022_CORE` | `null` — optional. Operating system family _(templateable)_

### EcsVolume

ECS volume — host path or Docker volume

- **`name`**: string — required. Volume name — referenced by container mount points _(templateable)_
- **`host_path`**: string \| null — optional. Host path for bind mounts (EC2 only) _(templateable)_
- **`efs_volume_configuration`**: [EcsEfsVolumeConfiguration](#ecsefsvolumeconfiguration) — optional. EFS volume configuration _(templateable)_

### EcsEnvironmentFile

Environment file from S3

- **`value`**: string — required. S3 ARN of the environment file _(templateable)_
- **`type`**: `s3` — required. File type _(templateable)_

### EcsEnvironmentVariable

Environment variable as name-value pair

- **`name`**: string — required. Variable name _(templateable)_
- **`value`**: string — required. Variable value _(templateable)_

### EcsResourceRequirement

GPU or other resource requirement

- **`type`**: `GPU` | `InferenceAccelerator` — required. Resource type — currently only GPU _(templateable)_
- **`value`**: string — required. Number of resources _(templateable)_

### EcsAwsVpcConfiguration

AWS VPC configuration for `awsvpc`-mode tasks.

- **`subnets`**: string[] — required. Subnet IDs the task ENI is placed into. _(minItems:1; templateable)_
- **`security_groups`**: string[] \| null — optional. Security groups attached to the task ENI. _(templateable)_
- **`assign_public_ip`**: `ENABLED` | `DISABLED` | `null` — optional. Whether the task ENI gets a public IP. Required for Fargate tasks pulling images from non-VPC-routable registries (e.g. _(templateable)_

### EcsTaskManagedEBSVolumeConfiguration

AWS-managed Fargate ephemeral EBS volume configuration.

- **`role_arn`**: string — required. IAM role ARN AWS uses to provision the EBS volume on the customer's behalf. Required. _(templateable)_
- **`volume_type`**: string \| null — optional. EBS volume type (`gp3`, `gp2`, `io2`, `io1`, `sc1`, `st1`, `standard`). _(templateable)_
- **`size_in_gib`**: integer \| null — optional. Volume size (GiB). Range depends on `volume_type`. _(format:int32; templateable)_
- **`snapshot_id`**: string \| null — optional. Snapshot id to restore from. _(templateable)_
- **`iops`**: integer \| null — optional. Provisioned IOPS — required for `io1`/`io2`, optional for `gp3`. _(format:int32; templateable)_
- **`throughput`**: integer \| null — optional. Provisioned throughput (MiB/s) — `gp3` only. _(format:int32; templateable)_
- **`filesystem_type`**: string \| null — optional. Filesystem type to format the volume with (`ext3`, `ext4`, `xfs`, `ntfs`). Defaults per OS family. _(templateable)_
- **`kms_key_id`**: string \| null — optional. KMS key id (alias or ARN) for at-rest encryption. _(templateable)_
- **`encrypted`**: boolean \| null — optional. Whether the volume is encrypted at rest. _(templateable)_
- **`tag_specifications`**: [EcsEBSTagSpecification](#ecsebstagspecification)[] \| null — optional. Tag specifications applied to the EBS volume. _(templateable)_
- **`termination_policy`**: [EcsTaskManagedEBSVolumeTerminationPolicy](#ecstaskmanagedebsvolumeterminationpolicy) — optional. Termination policy — controls whether the volume is deleted when the task stops. _(templateable)_
- **`volume_initialization_rate`**: integer \| null — optional. Initialization rate (MiB/s) for snapshot-restored volumes — controls how fast blocks are pre-warmed. _(format:int32; templateable)_

### EcsContainerDependency

Container dependency — controls startup order

- **`container_name`**: string — required. Name of the dependency container _(templateable)_
- **`condition`**: `START` | `COMPLETE` | `SUCCESS` | `HEALTHY` — required. Condition to wait for _(templateable)_

### EcsFirelensConfiguration

Firelens log routing configuration

- **`type`**: `fluentbit` | `fluentd` — required. Router type _(templateable)_
- **`options`**: map&lt;string,string&gt; \| null — optional. Router options _(templateable)_

### EcsHealthCheck

Container health check configuration

- **`command`**: string[] — required. Command to run — prefix with CMD or CMD-SHELL _(templateable)_
- **`interval`**: integer \| null — optional. Interval between checks in seconds _(format:int32; templateable)_
- **`timeout`**: integer \| null — optional. Timeout for each check in seconds _(format:int32; templateable)_
- **`retries`**: integer \| null — optional. Number of retries before marking unhealthy _(format:int32; templateable)_
- **`start_period`**: integer \| null — optional. Grace period before health checks start in seconds _(format:int32; templateable)_

### EcsLinuxParameters

Linux-specific container settings

- **`init_process_enabled`**: boolean \| null — optional. Run an init process inside the container _(templateable)_
- **`capabilities`**: [EcsLinuxCapabilities](#ecslinuxcapabilities) — optional. Linux capabilities to add or drop _(templateable)_
- **`shared_memory_size`**: integer \| null — optional. Shared memory size in MiB _(format:int32; templateable)_
- **`tmpfs`**: [EcsTmpfs](#ecstmpfs)[] \| null — optional. Tmpfs mounts _(templateable)_

### EcsLogConfiguration

Log configuration for a container

- **`log_driver`**: `awslogs` | `awsfirelens` | `json-file` | `syslog` | `fluentd` | `splunk` — required. Log driver — awslogs for CloudWatch, awsfirelens for Firelens _(templateable)_
- **`options`**: map&lt;string,string&gt; \| null — optional. Driver-specific options _(templateable)_
- **`secret_options`**: [EcsSecret](#ecssecret)[] \| null — optional. Secret log options (ARN references) _(templateable)_

### EcsMountPoint

Container mount point referencing a volume

- **`source_volume`**: string — required. Name of the volume (must match a volume in the task definition) _(templateable)_
- **`container_path`**: string — required. Path inside the container to mount _(templateable)_
- **`read_only`**: boolean \| null — optional. Whether the mount is read-only _(templateable)_

### EcsPortMapping

Port mapping for a container

- **`container_port`**: integer — required. Port on the container _(format:int32; templateable)_
- **`host_port`**: integer \| null — optional. Port on the host (EC2) or omit for awsvpc _(format:int32; templateable)_
- **`protocol`**: `tcp` | `udp` | `null` — optional. Transport protocol _(templateable)_
- **`app_protocol`**: `http` | `http2` | `grpc` | `null` — optional. Application protocol for service connect _(templateable)_

### EcsRepositoryCredentials

Private registry credentials

- **`credentials_parameter`**: string — required. ARN of the Secrets Manager secret containing registry credentials _(templateable)_

### EcsSecret

Secret injected from Parameter Store or Secrets Manager

- **`name`**: string — required. Environment variable name in the container _(templateable)_
- **`value_from`**: string — required. Full ARN of the secret in Parameter Store or Secrets Manager _(templateable)_

### EcsSystemControl

System control — kernel parameter override

- **`namespace`**: string — required. Namespace (e.g., net.core.somaxconn) _(templateable)_
- **`value`**: string — required. Value to set _(templateable)_

### EcsVolumeFrom

Volume-from reference — mount volumes from another container

- **`source_container`**: string — required. Source container name _(templateable)_
- **`read_only`**: boolean \| null — optional. Whether the mount is read-only _(templateable)_

### EcsProxyConfigurationProperty

Proxy configuration key-value property

- **`name`**: string — required. Property name _(templateable)_
- **`value`**: string — required. Property value _(templateable)_

### EcsEfsVolumeConfiguration

EFS volume configuration for ECS tasks

- **`file_system_id`**: string — required. EFS file system ID _(templateable)_
- **`root_directory`**: string \| null — optional. Root directory on the EFS file system _(templateable)_
- **`transit_encryption`**: `ENABLED` | `DISABLED` | `null` — optional. Whether to enable IAM authorization _(templateable)_
- **`transit_encryption_port`**: integer \| null — optional. Port for transit encryption (default 2049) _(format:int32; templateable)_
- **`authorization_config`**: [EcsEfsAuthorizationConfig](#ecsefsauthorizationconfig) — optional. EFS access point ID _(templateable)_

### EcsEBSTagSpecification

Tag specification for an ECS-managed EBS volume.

- **`resource_type`**: `volume` — required. Resource type the tags apply to. Currently only `volume` is supported. _(templateable)_
- **`propagate_tags`**: `TASK_DEFINITION` | `SERVICE` | `NONE` | `null` — optional. Where the tags come from — explicit list, propagated from the task definition, or none. _(templateable)_
- **`tags`**: [EcsTag](#ecstag)[] \| null — optional. Explicit tags. _(templateable)_

### EcsTaskManagedEBSVolumeTerminationPolicy

Termination policy for an ECS-managed EBS volume.

- **`delete_on_termination`**: boolean — required. Whether to delete the volume when the task stops. _(templateable)_

### EcsLinuxCapabilities

Linux kernel capabilities

- **`add`**: string[] \| null — optional. Capabilities to add _(templateable)_
- **`drop`**: string[] \| null — optional. Capabilities to drop _(templateable)_

### EcsTmpfs

Tmpfs mount configuration

- **`container_path`**: string — required. Mount path in the container _(templateable)_
- **`size`**: integer — required. Maximum size in MiB _(format:int32; templateable)_
- **`mount_options`**: string[] \| null — optional. Mount options _(templateable)_

### EcsEfsAuthorizationConfig

EFS authorization config

- **`access_point_id`**: string \| null — optional. EFS access point ID _(templateable)_
- **`iam`**: `ENABLED` | `DISABLED` | `null` — optional. Whether to use the task execution role for EFS _(templateable)_

## UI

UI configuration for links, logs, and metrics on the module dashboard

### ModuleUI

UI configuration for the module.

- **`links`**: [UILink](#uilink)[] \| null — optional. Links to display, such as service URLs, documentation, etc. _(templateable)_
- **`metrics`**: [UIMetric](#uimetric)[] \| null — optional. Metrics charts displayed on the module dashboard. _(templateable)_

### UILink

A link displayed in the module UI.

- **`name`**: string — required. Display name for the link _(minLen:1; templateable)_
- **`href`**: string — required. URL — can use template syntax like &lt;&lt; output.xxx &gt;&gt; for dynamic values _(templateable)_

### UIMetric

A metric displayed in the module UI.

- **`id`**: string — required. Stable identifier for the metric, used as the React key on the dashboard and in the metric query cache key. Must be unique within a module's UI. _(minLen:1; templateable)_
- **`name`**: string — required. Display name for the metric _(minLen:1; templateable)_
- **`type`**: `line` | `bar` — required. Chart type for the metric _(templateable)_
- **`source`**: [MetricSource](#metricsource) — required. Data source for the metric. Discriminated union — the only `type` currently supported is `cloudwatch`. The aggregation period is chosen by the requester (fro... _(templateable)_

### MetricSource

CloudWatch-backed metric source. Module authors fill this in with literal values or templated references (e.g. `&lt;&lt; stack.output.* &gt;&gt;`). The server uses `aws_...

- **`type`**: `cloudwatch` — required. Source type discriminator _(templateable)_
- **`aws_account_id`**: string — required. Ravion AWS account reference used to resolve credentials for the CloudWatch call. Accepts either the Flightcontrol AWS account database ID (e.g. `awsact_123`... _(minLen:1; templateable)_
- **`region`**: string — required. AWS region the metric lives in. _(minLen:1; templateable)_
- **`namespace`**: string — required. CloudWatch namespace _(minLen:1; templateable)_
- **`name`**: string — required. CloudWatch metric name _(minLen:1; templateable)_
- **`statistic`**: string — required. CloudWatch statistic to apply _(minLen:1; templateable)_
- **`dimensions`**: map&lt;string,string&gt; — required. Dimensions used to scope the metric. Required — no longer derived from any ARN since the source no longer carries one. _(templateable)_

### MetricType

Allowed metric chart types.

Allowed values:

- `line`
- `bar`

### CloudWatchMetricSource

CloudWatch-backed metric source. Module authors fill this in with literal values or templated references (e.g. `&lt;&lt; stack.output.* &gt;&gt;`). The server uses `aws_...

- **`type`**: `cloudwatch` — required. Source type discriminator _(templateable)_
- **`aws_account_id`**: string — required. Ravion AWS account reference used to resolve credentials for the CloudWatch call. Accepts either the Flightcontrol AWS account database ID (e.g. `awsact_123`... _(minLen:1; templateable)_
- **`region`**: string — required. AWS region the metric lives in. _(minLen:1; templateable)_
- **`namespace`**: string — required. CloudWatch namespace _(minLen:1; templateable)_
- **`name`**: string — required. CloudWatch metric name _(minLen:1; templateable)_
- **`statistic`**: string — required. CloudWatch statistic to apply _(minLen:1; templateable)_
- **`dimensions`**: map&lt;string,string&gt; — required. Dimensions used to scope the metric. Required — no longer derived from any ARN since the source no longer carries one. _(templateable)_

## Readme

Human-readable module documentation in Markdown.

## Template expressions

Fields marked _templateable_ accept `<< expression >>` template tokens.

- Block mode preserves the resolved type:

  ```yaml
  timeout: << module.input.timeout >>
  ```

- Interpolation mode always produces a string:

  ```yaml
  name: "prefix-<< module.input.env >>"
  ```

Supported operators: `+`, `-`, `*`, `/`, `%`, `>`, `<`, `>=`, `<=`, `==`, `!=`, `&&`, `||`, `!`.

Ternary: `<< condition ? trueVal : falseVal >>`. Default fallback: `` << input.x || "fallback" >> ``.

Access: `array[0]`, `map["key"]`, `map.key`. Functions: `len()`, `upper()`, `lower()`.

Available namespaces: `module.input.*`, `stack.id`, `trigger.payload.{branch,commit,ref,repository,event,tag}`.
