{/* Generated by scripts/sync-schema-reference.mjs from ../flightcontrol2/packages/schemas/schema_reference/docs/pipeline.md. Do not edit by hand. */}

The pipeline configuration schema. A pipeline defines variants, triggers, inputs, and an ordered list of steps to execute.

## Legend

| Notation | Meaning |
| --- | --- |
| `T[]` | Array of `T` |
| `map<string,T>` | Object keyed by strings with `T` values |
| `enum[a,b]` | One of the listed values |
| `A \| B` | One of several types |
| ~template | Field accepts `<< ... >>` template expressions |

## PipelineConfig

Pipeline configuration - the top-level schema

- **`triggers`**: [PipelineTrigger](#pipelinetrigger)[] — optional. Webhook triggers that automatically start pipeline runs on events like git push
- **`inputs`**: [PipelineInputDefinition](#pipelineinputdefinition)[] — optional. User-configurable inputs that can vary per run — environment name, image tag, feature flags, etc
- **`variants`**: [VariantDefinition](#variantdefinition)[] — required. Deployment variants such as production, staging, or dev — each variant can lock specific input values
- **`steps`**: [Step](#step)[] — required. Ordered list of steps to execute — steps run sequentially unless wrapped in a parallel block _(minItems:1)_
- **`rollback`**: [Step](#step)[] — optional. Steps to execute if the pipeline fails — runs in order after a failure is detected

## Triggers

Webhook triggers that automatically start pipeline runs on events like git push

### PipelineTrigger

Pipeline trigger definition.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `webhook` | [GenericPipelineTrigger](#genericpipelinetrigger) |
| `"webhook:github"` | [GithubPipelineTrigger](#githubpipelinetrigger) |
| `"webhook:gitlab"` | [GitlabPipelineTrigger](#gitlabpipelinetrigger) |

### GenericPipelineTrigger

Pipeline trigger for generic webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [TriggerFilter](#triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook` — required. Webhook type for generic webhook triggers.

### GithubPipelineTrigger

Pipeline trigger for GitHub webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [TriggerFilter](#triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook:github` — required. Webhook type — determines which events are supported.
- **`event`**: `push` | `pull_request` | `pull_request_review` | `pull_request_review_comment` | `pull_request_review_thread` | `release` | `deployment` — required. Webhook event type to listen for.

### GitlabPipelineTrigger

Pipeline trigger for GitLab webhooks.

- **`id`**: string — required. Unique identifier for this trigger
- **`repo`**: string — required. Git repository in owner/repo format _(templateable)_
- **`filter`**: [TriggerFilter](#triggerfilter) — optional. Filters to narrow which events trigger the pipeline
- **`run`**: map&lt;string,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; — required. Maps variant IDs to run configuration — keys must match declared variant IDs.
- **`type`**: `webhook:gitlab` — required. Webhook type — determines which events are supported.
- **`event`**: `push` | `merge_request` | `tag_push` — required. Webhook event type to listen for.

### TriggerFilter

Trigger filter configuration

- **`branch`**: string \| null — optional. Only trigger when this branch is pushed _(templateable)_
- **`tag`**: string \| null — optional. Only trigger when a tag matching this pattern is pushed _(templateable)_
- **`action`**: string \| string[] — optional. Only trigger for specific webhook actions. _(templateable)_
- **`paths`**: string[] \| null — optional. Only trigger when files in these paths are changed _(templateable)_
- **`payload`**: [PayloadCondition](#payloadcondition)[] — optional. Filter by webhook payload fields using dot-notation paths.

### TriggerRunVariantConfig

Per-variant run configuration for a trigger.

- **`description`**: string \| null — optional. Description for the pipeline run created by this trigger variant _(templateable)_
- **`input`**: map&lt;string,any \| null&gt; — optional. Input overrides for this variant run _(templateable)_

### GithubWebhookEvent

GitHub webhook events that can trigger a pipeline

Allowed values:

- `push`
- `pull_request`
- `pull_request_review`
- `pull_request_review_comment`
- `pull_request_review_thread`
- `release`
- `deployment`

### GitlabWebhookEvent

GitLab webhook events that can trigger a pipeline

Allowed values:

- `push`
- `merge_request`
- `tag_push`

### PayloadCondition

Condition to check against a webhook payload field.

- **`path`**: string — required. Dot-notation path to the payload field
- **`equals`**: string \| null — optional. Exact string match _(templateable)_
- **`contains`**: string \| null — optional. Substring match _(templateable)_
- **`matches`**: string \| null — optional. Glob pattern match _(templateable)_
- **`one_of`**: string[] \| null — optional. Value must be one of these strings _(templateable)_
- **`not`**: [PayloadConditionMatcher](#payloadconditionmatcher) — optional. Negated payload field matcher. The condition fails when this matcher is true.

### PayloadConditionMatcher

Payload field matcher used by positive conditions and negated conditions.

- **`equals`**: string \| null — optional. Exact string match _(templateable)_
- **`contains`**: string \| null — optional. Substring match _(templateable)_
- **`matches`**: string \| null — optional. Glob pattern match _(templateable)_
- **`one_of`**: string[] \| null — optional. Value must be one of these strings _(templateable)_

## Inputs

User-configurable inputs that can vary per run — environment name, image tag, feature flags, etc

### PipelineInputDefinition

Union of all pipeline input definitions, discriminated by `type`.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `boolean` | [PipelineBooleanInputDefinition](#pipelinebooleaninputdefinition) |
| `number` | [PipelineNumberInputDefinition](#pipelinenumberinputdefinition) |
| `object` | [PipelineObjectInputDefinition](#pipelineobjectinputdefinition) |
| `string` | [PipelineStringInputDefinition](#pipelinestringinputdefinition) |
| `string_array` | [PipelineStringArrayInputDefinition](#pipelinestringarrayinputdefinition) |
| `text` | [PipelineTextInputDefinition](#pipelinetextinputdefinition) |

### PipelineBooleanInputDefinition

Boolean pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `boolean` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: boolean — optional. Default value used when no value is provided at runtime

### PipelineNumberInputDefinition

Number pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `number` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: integer — optional. Default value used when no value is provided at runtime _(format:int32)_
- **`values`**: [Module.NumberValueItem](#module-numbervalueitem)[] — optional. Dropdown options restricting allowed values
- **`min`**: integer — optional. Minimum allowed value (inclusive) _(format:int32)_
- **`max`**: integer — optional. Maximum allowed value (inclusive) _(format:int32)_

### PipelineObjectInputDefinition

Object pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `object` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: any — optional. Default value used when no value is provided at runtime

### PipelineStringArrayInputDefinition

String array pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `string_array` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: string[] — optional. Default value used when no value is provided at runtime
- **`patterns`**: [Module.ValidationPattern](#module-validationpattern)[] — optional. Regex patterns each array element must match

### PipelineStringInputDefinition

String pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `string` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: string — optional. Default value used when no value is provided at runtime
- **`values`**: [Module.StringValueItem](#module-stringvalueitem)[] — optional. Dropdown options restricting allowed values
- **`patterns`**: [Module.ValidationPattern](#module-validationpattern)[] — optional. Regex patterns the value must match

### PipelineTextInputDefinition

Text pipeline input definition

- **`id`**: string — required. Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_
- **`type`**: `text` — required. Input type — determines the field widget and validation rules
- **`required`**: boolean — optional, default `true`. Whether this input must be provided when running the pipeline
- **`description`**: string — optional. Human-readable description shown in the UI when running the pipeline
- **`default`**: string — optional. Default value used when no value is provided at runtime
- **`patterns`**: [Module.ValidationPattern](#module-validationpattern)[] — optional. Regex patterns the value must match

### Module.NumberValueItem

A selectable option with a numeric value.

- **`label`**: string — optional. Text shown in the dropdown
- **`description`**: string — optional. Extra detail shown alongside the option
- **`group_label`**: string — optional. Groups options under a shared heading in the dropdown
- **`value`**: integer — required. Actual number sent to Terraform when this option is selected _(format:int32)_

### Module.ValidationPattern

A regex pattern with an error message for validation

- **`pattern`**: string — required. JavaScript regex the input must match
- **`message`**: string — required. Error shown to the user when validation fails

### Module.StringValueItem

A selectable option with a string value.

- **`label`**: string — optional. Text shown in the dropdown
- **`description`**: string — optional. Extra detail shown alongside the option
- **`group_label`**: string — optional. Groups options under a shared heading in the dropdown
- **`value`**: string — required. Actual value sent to Terraform when this option is selected

## Variants

Deployment variants such as production, staging, or dev — each variant can lock specific input values

### VariantDefinition

Variant definition

- **`id`**: string — required. Unique identifier for this variant, referenced by triggers _(minLen:1)_
- **`name`**: string — required. Display name shown in the UI _(minLen:1)_
- **`input`**: map&lt;string,any \| null&gt; — optional. Hard-coded input values for this variant — keys must match declared inputs, cannot be overridden by triggers

## Steps

Ordered list of steps to execute — steps run sequentially unless wrapped in a parallel block

### Step

Step - can be action, parallel, or group.

One of: [ActionStep](#actionstep), [ParallelStep](#parallelstep), [GroupStep](#groupstep).

### ActionStep

Union of all action step types.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `approval` | [ApprovalStep](#approvalstep) |
| `build` | [BuildStep](#buildstep) |
| `"build:image"` | [BuildImageCiStep](#buildimagecistep) |
| `"build:static"` | [BuildStaticCiStep](#buildstaticcistep) |
| `custom` | [CustomCommandCiStep](#customcommandcistep) |
| `deploy` | [DeployStep](#deploystep) |
| `"deploy:swap-blue-green"` | [DeploySwapBlueGreenStep](#deployswapbluegreenstep) |
| `rollback` | [RollbackStep](#rollbackstep) |
| `sleep` | [SleepStep](#sleepstep) |
| `"terraform:apply"` | [TerraformApplyCiStep](#terraformapplycistep) |
| `"terraform:plan"` | [TerraformPlanCiStep](#terraformplancistep) |
| `"trigger:pipeline"` | [TriggerPipelineStep](#triggerpipelinestep) |

### GroupStep

Group step - contains nested steps

- **`group`**: string — required. Human-readable group label shown in pipeline UI
- **`steps`**: [Step](#step)[] — required _(minItems:1)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for executions within this group

### ParallelStep

Parallel step - executes children concurrently

- **`parallel`**: [ParallelChild](#parallelchild)[] — required _(minItems:1)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for executions within this parallel block
- **`fail_strategy`**: `cancel-all` | `finish-running` | `finish-all` — optional, default `cancel-all`. How to handle failures while branches in this parallel block are running

### ApprovalStep

Approval action step — pauses pipeline execution until a user explicitly approves

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `approval` — required

### BuildImageCiStep

Build image CI action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `build:image` — required
- **`source`**: [GitSource](#gitsource) — required. Git source configuration _(templateable)_
- **`builder`**: [ImageBuildConfig](#imagebuildconfig) — required. Builder configuration — dockerfile or nixpacks settings for image builds _(templateable)_
- **`environment_variables`**: [EnvironmentVariablesValue](#environmentvariablesvalue) — optional. Environment variables available during the build — plain strings or AWS secret references _(templateable)_
- **`debug`**: boolean \| null — optional. Enable verbose build output for debugging build issues Accepts either a boolean literal or a full template token _(templateable)_
- **`destinations`**: [EcrDestination](#ecrdestination)[] — required. ECR destinations where the built Docker image will be pushed _(minItems:1; templateable)_
- **`infrastructure`**: [StepInfrastructure](#stepinfrastructure) — required. Execution environment configuration — required because image builds run on provisioned compute _(templateable)_

### BuildStaticCiStep

Build static CI action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `build:static` — required
- **`source`**: [GitSource](#gitsource) — required. Git source configuration _(templateable)_
- **`builder`**: [StaticBuildConfig](#staticbuildconfig) — required. Builder configuration — dockerfile or nixpacks settings _(templateable)_
- **`environment_variables`**: [EnvironmentVariablesValue](#environmentvariablesvalue) — optional. Environment variables available during the build — plain strings or AWS secret references _(templateable)_
- **`debug`**: boolean \| null — optional. Enable verbose build output for debugging build issues Accepts either a boolean literal or a full template token _(templateable)_
- **`destinations`**: [S3StaticDestination](#s3staticdestination)[] — required. S3 destinations where the built static assets will be uploaded _(minItems:1; templateable)_
- **`infrastructure`**: [StepInfrastructure](#stepinfrastructure) — required. Execution environment configuration — required because static builds run on provisioned compute _(templateable)_

### BuildStep

General-purpose build action step — triggers a build based on the module's build config.

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`module_instance`**: string — required. Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_
- **`description`**: string \| null — optional. Human-readable description of this deployment. _(templateable)_
- **`type`**: `build` — required
- **`input`**: map&lt;string,any \| null&gt; — optional. User-defined build parameters passed through to the module build manager. _(templateable)_

### CustomCommandCiStep

Custom command CI action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`setup`**: [SetupAction](#setupaction)[] \| null — optional. Setup actions to install tools before the commands run, in order, before any command. _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `custom` — required
- **`source`**: [GitSource](#gitsource) — optional. Git source configuration _(templateable)_
- **`commands`**: string[] — required. Shell commands to execute in order _(minItems:1; templateable)_
- **`environment_variables`**: [EnvironmentVariablesValue](#environmentvariablesvalue) — optional. Environment variables available during command execution _(templateable)_
- **`infrastructure`**: [StepInfrastructure](#stepinfrastructure) — required. Execution environment configuration — required because custom commands run on provisioned compute _(templateable)_

### DeployStep

General-purpose deploy action step — triggers a deployment based on the module's deployment config.

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`module_instance`**: string — required. Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_
- **`description`**: string \| null — optional. Human-readable description of this deployment. _(templateable)_
- **`type`**: `deploy` — required
- **`input`**: map&lt;string,any \| null&gt; — optional. User-defined deploy parameters passed through to the deploy manager. _(templateable)_

### DeploySwapBlueGreenStep

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`module_instance`**: string — required. Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_
- **`description`**: string \| null — optional. Human-readable description of this deployment. _(templateable)_
- **`type`**: `deploy:swap-blue-green` — required
- **`input`**: map&lt;string,any \| null&gt; — optional _(templateable)_

### RollbackStep

Rollback action step — reverts a module to a specific previous deployment.

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`type`**: `rollback` — required
- **`deployment_id`**: string — required. Deployment ID to rollback to _(minLen:1; templateable)_

### SleepStep

Sleep action step — pauses pipeline execution for testing and debugging

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `sleep` — required
- **`duration_ms`**: integer — required. Sleep duration in milliseconds _(format:int64; templateable)_

### TerraformApplyCiStep

Terraform apply CI action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `terraform:apply` — required
- **`source`**: [GitSource](#gitsource) — optional. Git source configuration — if omitted, uses the trigger's repository _(templateable)_
- **`environment_variables`**: [EnvironmentVariablesValue](#environmentvariablesvalue) — optional. Environment variables available during execution — plain strings or AWS secret references _(templateable)_
- **`tool`**: `opentofu` | `terraform` — required. IaC tool implementation used to execute this step _(templateable)_
- **`version`**: string — required. Terraform/OpenTofu version to execute _(minLen:1; templateable)_
- **`stack_id`**: string \| null — optional. Stack ID used for terraform resource/execution tracking Automatically set when triggered from a stack _(templateable)_
- **`plan_file_uri`**: string — required. S3 URI of the plan file to apply — typically from a preceding plan step output (e.g. s3://bucket/path/to/plan.tfplan) _(templateable)_
- **`infrastructure`**: [StepInfrastructure](#stepinfrastructure) — required. Execution environment configuration — required because Terraform apply runs on provisioned compute _(templateable)_

### TerraformPlanCiStep

Terraform plan CI action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `terraform:plan` — required
- **`source`**: [GitSource](#gitsource) — optional. Git source configuration — if omitted, uses the trigger's repository _(templateable)_
- **`environment_variables`**: [EnvironmentVariablesValue](#environmentvariablesvalue) — optional. Environment variables available during execution — plain strings or AWS secret references _(templateable)_
- **`tool`**: `opentofu` | `terraform` — required. IaC tool implementation used to execute this step _(templateable)_
- **`version`**: string — required. Terraform/OpenTofu version to execute _(minLen:1; templateable)_
- **`stack_id`**: string \| null — optional. Stack ID used for terraform resource/execution tracking Automatically set when triggered from a stack _(templateable)_
- **`terraform_variables`**: [TerraformVariablesValue](#terraformvariablesvalue) — optional. Terraform input variables written to a .auto.tfvars.json file during plan creation — supports all HCL types and AWS secret references _(templateable)_
- **`terraform_variable_files`**: string[] \| null — optional. Terraform tfvars file paths passed to terraform plan as -var-file flags, in the configured order. _(templateable)_
- **`plan_file_directory_uri`**: string \| null — optional. S3 directory URI to upload the plan file to — overrides the default convention-based path (e.g. s3://bucket/terraform-plans/run-123) _(templateable)_
- **`plan_type`**: `apply` | `destroy` — optional, default `apply`. Type of plan to generate — "apply" (default) creates/updates resources, "destroy" tears them down _(templateable)_
- **`infrastructure`**: [StepInfrastructure](#stepinfrastructure) — required. Execution environment configuration — required because Terraform plan runs on provisioned compute _(templateable)_

### TriggerPipelineStep

Trigger pipeline action step

- **`id`**: string — required. Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_
- **`name`**: string — optional. Human-readable step name shown in the pipeline UI
- **`timeout`**: integer \| null — optional. Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_
- **`if`**: boolean \| null — optional. Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_
- **`concurrency`**: [ConcurrencyConfig](#concurrencyconfig) — optional. Concurrency configuration for controlling step execution across variants
- **`type`**: `trigger:pipeline` — required
- **`pipeline_id`**: string — required. ID of the pipeline to trigger _(minLen:1; templateable)_
- **`pipeline_version_id`**: string \| null — optional. Specific pipeline version to trigger _(templateable)_
- **`description`**: string — required. Description to use for the triggered pipeline run _(minLen:1; templateable)_
- **`inputs`**: map&lt;string,any \| null&gt; — optional. Input values to pass to the triggered pipeline as key-value pairs _(templateable)_
- **`run`**: map&lt;string,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; \| null — optional. Per-variant run overrides — keys are variant IDs, values include description and optional input overrides _(templateable)_

### ConcurrencyConfig

Concurrency configuration for controlling parallel execution

- **`key`**: string — optional. Unique identifier for the concurrency scope within this pipeline Steps/blocks with the same key share a concurrency limit Keys are scoped to org + pipeline —...
- **`value`**: integer — optional, default `0`. Maximum concurrent executions allowed (0 means unlimited, 1 means sequential) _(format:int32,min:0)_
- **`behavior`**: `queue` | `cancel-in-progress` — optional, default `queue`. Behavior when the concurrency limit is reached
- **`queue_size`**: integer — optional. Maximum queue size when behavior is "queue" Only applies when behavior is "queue" Unset means unlimited queue depth 0 disables queueing when behavior is "queue" _(format:int32,min:0)_
- **`queue_overflow_discard`**: `oldest` | `newest` — optional, default `oldest`. Which items to discard when queue overflows Only applies when behavior is "queue" and queue_size &gt; 0

### ParallelChild

Parallel child - can be action or group (no nested parallel allowed)

One of: [ActionStep](#actionstep), [GroupStep](#groupstep).

### ParallelFailStrategy

Strategy for handling failures within a parallel block

Allowed values:

- `cancel-all`
- `finish-running`
- `finish-all`

### EcrDestination

ECR destination for image builds

- **`id`**: string — required. Unique destination identifier within the build step _(templateable)_
- **`type`**: `ecr` — required. ECR destination type _(templateable)_
- **`repository_arn`**: string — required. ECR repository ARN _(templateable)_
- **`tags`**: string[] \| null — optional. Docker image tags to apply in ECR after the build finishes _(templateable)_

### EnvironmentVariablesValue

Environment variable map value.

One of: map&lt;string,[EnvironmentVariable](#environmentvariable)&gt;, string.

### GitSource

Git source configuration for fetching code

- **`type`**: `git` — required. Source type identifier _(templateable)_
- **`repo`**: string — required. Full repository URL including host _(templateable)_
- **`branch`**: string — required. Branch to check out _(minLen:1; templateable)_
- **`ref`**: string \| null — optional. Git ref to check out — branch, commit SHA, tag, or other ref. Overrides branch HEAD if set. _(templateable)_
- **`base_path`**: string \| null — optional, default `.`. Base path within the repository to use as the working root _(templateable)_

### ImageBuildConfig

Image build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `disabled` | [DisabledBuildConfig](#disabledbuildconfig) |
| `dockerfile` | [ImageDockerfileBuildConfig](#imagedockerfilebuildconfig) |
| `nixpacks` | [ImageNixpacksBuildConfig](#imagenixpacksbuildconfig) |

### StepInfrastructure

Step infrastructure configuration

- **`execution_environment_id`**: string \| null — optional. ID or given ID of a pre-configured execution environment _(templateable)_
- **`aws_account_id`**: string \| null — optional. AWS account reference for automatic network selection. _(templateable)_
- **`region`**: string \| null — optional. AWS region for automatic network selection _(templateable)_
- **`type`**: `ec2` | `ec2-spot` — required. Instance type — "ec2" for on-demand or "ec2-spot" for cheaper spot instances _(templateable)_
- **`instance_size`**: string \| string[] — required. Instance size — a single size like "large" or an array like ["large", "xlarge"] for spot fallback _(templateable)_
- **`ami`**: string \| null — optional. AMI ID for the runner instance _(templateable)_
- **`permissions`**: [AwsPermissions](#awspermissions) — optional. IAM permissions for the runner role — "attach" to add policies or "replace" to override step defaults _(templateable)_
- **`storage`**: [StorageConfig](#storageconfig) — optional. Root volume storage configuration _(templateable)_

### S3StaticDestination

S3 destination for static builds

- **`id`**: string — required. Unique destination identifier within the build step _(minLen:1; templateable)_
- **`type`**: `s3` — required. S3 destination type _(templateable)_
- **`bucket`**: string — required. S3 bucket name where built assets will be uploaded _(templateable)_
- **`directory`**: string \| null — optional. S3 directory override for the uploaded build output. _(templateable)_
- **`region`**: string — required. AWS region for the destination S3 bucket _(templateable)_

### StaticBuildConfig

Static build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `dockerfile` | [StaticDockerfileBuildConfig](#staticdockerfilebuildconfig) |
| `nixpacks` | [StaticNixpacksBuildConfig](#staticnixpacksbuildconfig) |

### SetupAction

A setup action that installs a tool before the step's commands run.

- **`uses`**: string — required. Action reference to install a tool before running commands. _(minLen:1; templateable)_
- **`with`**: map&lt;string,string&gt; \| null — optional. Inputs passed to the action, keyed by the action's declared input names. _(templateable)_

### TerraformTool

Supported IaC tool for terraform:* CI steps

Allowed values:

- `opentofu`
- `terraform`

### TerraformPlanType

Type of Terraform plan to generate

Allowed values:

- `apply`
- `destroy`

### TerraformVariableFilePath

Terraform/OpenTofu tfvars file path relative to the step source base path

Type: string

### TerraformVariablesValue

Terraform variable map value.

One of: map&lt;string,[TerraformVariable](#terraformvariable)&gt;, string.

### ConcurrencyBehavior

Behavior when the concurrency limit is reached

Allowed values:

- `queue`
- `cancel-in-progress`

### QueueOverflowDiscard

Queue overflow discard strategy

Allowed values:

- `oldest`
- `newest`

### EnvironmentVariable

Environment variable — can be a plain string, Parameter Store reference, or Secrets Manager reference

One of: string \| null, [ParameterStoreEnvVar](#parameterstoreenvvar), [SecretsManagerEnvVar](#secretsmanagerenvvar).

### GitRepoUrl

Full git repository URL including host.

Type: string

### DisabledBuildConfig

Disabled build configuration. Valid only for module-driven builds.

- **`type`**: `disabled` — required. Build method — disables module build execution _(templateable)_

### ImageDockerfileBuildConfig

Dockerfile configuration for image builds

- **`type`**: `dockerfile` — required. Build method — use a custom Dockerfile _(templateable)_
- **`dockerfile`**: string \| null — optional, default `Dockerfile`. Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_
- **`context`**: string \| null — optional, default `.`. Docker build context path relative to source.base_path _(templateable)_
- **`inject_env_variables_in_dockerfile`**: boolean \| null — optional. Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_
- **`cache_from`**: [CacheFromConfig](#cachefromconfig) — optional. Docker layer cache configuration _(templateable)_

### ImageNixpacksBuildConfig

Nixpacks configuration for image builds

- **`type`**: `nixpacks` — required. Build method — use Nixpacks auto-detection _(templateable)_
- **`nixpacks_version`**: string \| null — optional. Nixpacks version to use _(templateable)_
- **`build_path`**: string \| null — optional. Directory to run the Nixpacks build from, relative to the repository root _(templateable)_
- **`config_file_path`**: string \| null — optional. Path to the Nixpacks configuration file relative to the repository root _(templateable)_
- **`nix_pkgs`**: string[] \| null — optional. Additional Nix packages to install _(templateable)_
- **`apt_pkgs`**: string[] \| null — optional. Additional apt packages to install _(templateable)_
- **`nix_libs`**: string[] \| null — optional. Additional Nix libraries to install _(templateable)_
- **`install_cmd`**: string \| null — optional. Custom install command _(templateable)_
- **`build_cmd`**: string \| null — optional. Custom build command _(templateable)_
- **`start_cmd`**: string \| null — optional. Application start command _(templateable)_
- **`cache_from`**: [CacheFromConfig](#cachefromconfig) — optional. Docker layer cache configuration _(templateable)_

### AwsPermissions

IAM policies configuration for runner roles Used by EC2-backed steps to specify additional permissions

One of: [AwsPermissionsAttach](#awspermissionsattach), [AwsPermissionsReplace](#awspermissionsreplace).

### InfrastructureType

Execution environment type

Allowed values:

- `ec2`
- `ec2-spot`

### InstanceSize

Instance size can be a single string or array of strings

One of: string, string[].

### StorageConfig

Root volume storage configuration

- **`type`**: `gp3` | `gp2` | `io2` | `io1` — optional. Volume type _(templateable)_
- **`size`**: integer \| null — optional. Volume size in GiB _(format:int32; templateable)_
- **`iops`**: integer \| null — optional. Provisioned IOPS _(format:int32; templateable)_
- **`throughput`**: integer \| null — optional. Provisioned throughput in MiB/s _(format:int32; templateable)_

### StaticDockerfileBuildConfig

Dockerfile configuration for static builds

- **`type`**: `dockerfile` — required. Build method — use a custom Dockerfile _(templateable)_
- **`dockerfile`**: string \| null — optional, default `Dockerfile`. Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_
- **`context`**: string \| null — optional, default `.`. Docker build context path relative to source.base_path _(templateable)_
- **`inject_env_variables_in_dockerfile`**: boolean \| null — optional. Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_
- **`output_directory`**: string — required. Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_

### StaticNixpacksBuildConfig

Nixpacks configuration for static builds

- **`type`**: `nixpacks` — required. Build method — use Nixpacks auto-detection _(templateable)_
- **`nixpacks_version`**: string \| null — optional. Nixpacks version to use _(templateable)_
- **`build_path`**: string \| null — optional. Directory to run the Nixpacks build from, relative to the repository root _(templateable)_
- **`config_file_path`**: string \| null — optional. Path to the Nixpacks configuration file relative to the repository root _(templateable)_
- **`nix_pkgs`**: string[] \| null — optional. Additional Nix packages to install _(templateable)_
- **`apt_pkgs`**: string[] \| null — optional. Additional apt packages to install _(templateable)_
- **`nix_libs`**: string[] \| null — optional. Additional Nix libraries to install _(templateable)_
- **`install_cmd`**: string \| null — optional. Custom install command _(templateable)_
- **`build_cmd`**: string \| null — optional. Custom build command _(templateable)_
- **`output_directory`**: string — required. Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_

### TerraformVariable

Terraform variable — can be any JSON value (string, number, boolean, null, array, object), a Parameter Store reference, or a Secrets Manager reference

One of: [ParameterStoreEnvVar](#parameterstoreenvvar), [SecretsManagerEnvVar](#secretsmanagerenvvar), any \| null.

### ParameterStoreEnvVar

Environment variable from AWS Parameter Store

- **`from_parameter_store`**: [ParameterStoreReferenceValue](#parameterstorereferencevalue) — required. Parameter Store reference _(templateable)_

### SecretsManagerEnvVar

Environment variable from AWS Secrets Manager

- **`from_secrets_manager`**: [SecretsManagerReferenceValue](#secretsmanagerreferencevalue) — required. Secrets Manager reference _(templateable)_

### CacheFromConfig

Configuration for Docker layer cache source from ECR

- **`digest`**: string \| null — optional. Specific image digest to use for cache _(templateable)_
- **`tag`**: string \| null — optional. Specific image tag to use for cache _(templateable)_

### AwsPermissionsAttach

IAM policies to attach alongside step default policies

- **`attach`**: string[] — required. AWS managed policy ARNs to attach alongside step default policies _(templateable)_

### AwsPermissionsReplace

IAM policies to replace step default policies entirely

- **`replace`**: string[] — required. AWS managed policy ARNs to use instead of step default policies _(templateable)_

### EbsVolumeType

EBS volume type for the runner root volume

Allowed values:

- `gp3`
- `gp2`
- `io2`
- `io1`

### ParameterStoreReferenceValue

Parameter Store reference — shorthand string or object with optional version metadata

One of: string, [ParameterStoreReference](#parameterstorereference).

### SecretsManagerReferenceValue

Secrets Manager reference — shorthand string or object with optional JSON key/version metadata

One of: string, [SecretsManagerReference](#secretsmanagerreference).

### ParameterStoreReference

Parameter Store reference with optional version metadata

- **`key`**: string — required. Parameter Store parameter name or path _(templateable)_
- **`version`**: string \| null — optional. Parameter version number _(templateable)_

### SecretsManagerReference

Secrets Manager reference with optional JSON key and version metadata

- **`key`**: string — required. Secret name or ARN in Secrets Manager _(templateable)_
- **`json_key`**: string \| null — optional. JSON key to extract from a JSON-structured secret _(templateable)_
- **`version`**: string \| null — optional. Secret version ID or stage _(templateable)_

## Rollback

Steps to execute if the pipeline fails — runs in order after a failure is detected

Uses the same types as [Steps](#steps).

## Template expressions

Fields marked _templateable_ accept `<< expression >>` template tokens.

- Block mode preserves the resolved type:

  ```yaml
  timeout: << pipeline.input.timeout >>
  ```

- Interpolation mode always produces a string:

  ```yaml
  name: "prefix-<< pipeline.input.env >>"
  ```

Supported operators: `+`, `-`, `*`, `/`, `%`, `>`, `<`, `>=`, `<=`, `==`, `!=`, `&&`, `||`, `!`.

Ternary: `<< condition ? trueVal : falseVal >>`. Default fallback: `` << input.x || "fallback" >> ``.

Access: `array[0]`, `map["key"]`, `map.key`. Functions: `len()`, `upper()`, `lower()`.

Available namespaces: `pipeline.input.*`, `pipeline.run.{id,description}`, `pipeline.variant.{id,name}`, `steps.{id}.{output,input}.*`, `step.input.*`, `trigger.payload.{branch,commit,ref,repository,event,tag}`.
