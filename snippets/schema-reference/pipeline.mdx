{/* Generated by scripts/sync-schema-reference.mjs from ../flightcontrol2/packages/schemas/schema_reference/docs/pipeline.md. Do not edit by hand. */}

The pipeline configuration schema. A pipeline defines variants, triggers, inputs, and an ordered list of steps to execute.

## Legend

| Notation | Meaning |
| --- | --- |
| `T[]` | Array of `T` |
| `map<str,T>` | Object keyed by strings with `T` values |
| `enum[a,b]` | One of the listed values |
| `A \| B` | One of several types |
| ~template | Field accepts `<< ... >>` template expressions |

## PipelineConfig

Pipeline configuration - the top-level schema

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `variants` | [VariantDefinition](#variantdefinition)[] | Yes | — | Deployment variants such as production, staging, or dev — each variant can lock specific input values |
| `triggers` | [PipelineTrigger](#pipelinetrigger)[] | No | — | Webhook triggers that automatically start pipeline runs on events like git push |
| `inputs` | [PipelineInputDefinition](#pipelineinputdefinition)[] | No | — | User-configurable inputs that can vary per run — environment name, image tag, feature flags, etc |
| `steps` | [Step](#step)[] | Yes | — | Ordered list of steps to execute — steps run sequentially unless wrapped in a parallel block _(minItems:1)_ |
| `rollback` | [Step](#step)[] | No | — | Steps to execute if the pipeline fails — runs in order after a failure is detected |

## PipelineInputDefinition

Union of all pipeline input definitions, discriminated by `type`.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `boolean` | [PipelineBooleanInputDefinition](#pipelinebooleaninputdefinition) |
| `number` | [PipelineNumberInputDefinition](#pipelinenumberinputdefinition) |
| `object` | [PipelineObjectInputDefinition](#pipelineobjectinputdefinition) |
| `string` | [PipelineStringInputDefinition](#pipelinestringinputdefinition) |
| `string_array` | [PipelineStringArrayInputDefinition](#pipelinestringarrayinputdefinition) |
| `text` | [PipelineTextInputDefinition](#pipelinetextinputdefinition) |

## PipelineTrigger

Pipeline trigger definition.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `webhook` | [GenericPipelineTrigger](#genericpipelinetrigger) |
| `"webhook:github"` | [GithubPipelineTrigger](#githubpipelinetrigger) |
| `"webhook:gitlab"` | [GitlabPipelineTrigger](#gitlabpipelinetrigger) |

## Step

Step - can be action, parallel, or group.

One of: [ActionStep](#actionstep), [ParallelStep](#parallelstep), [GroupStep](#groupstep).

## VariantDefinition

Variant definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this variant, referenced by triggers _(minLen:1)_ |
| `name` | str | Yes | — | Display name shown in the UI _(minLen:1)_ |
| `input` | map&lt;str,any \| null&gt; | No | — | Hard-coded input values for this variant — keys must match declared inputs, cannot be overridden by triggers |

## PipelineBooleanInputDefinition

Boolean pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | boolean | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | bool | No | — | Default value used when no value is provided at runtime |

## PipelineNumberInputDefinition

Number pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | number | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | int | No | — | Default value used when no value is provided at runtime _(format:int32)_ |
| `values` | [Module.NumberValueItem](#module-numbervalueitem)[] | No | — | Dropdown options restricting allowed values |
| `min` | int | No | — | Minimum allowed value (inclusive) _(format:int32)_ |
| `max` | int | No | — | Maximum allowed value (inclusive) _(format:int32)_ |

## PipelineObjectInputDefinition

Object pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | object | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | any | No | — | Default value used when no value is provided at runtime |

## PipelineStringArrayInputDefinition

String array pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | string_array | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | str[] | No | — | Default value used when no value is provided at runtime |
| `patterns` | [Module.ValidationPattern](#module-validationpattern)[] | No | — | Regex patterns each array element must match |

## PipelineStringInputDefinition

String pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | string | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | str | No | — | Default value used when no value is provided at runtime |
| `values` | [Module.StringValueItem](#module-stringvalueitem)[] | No | — | Dropdown options restricting allowed values |
| `patterns` | [Module.ValidationPattern](#module-validationpattern)[] | No | — | Regex patterns the value must match |

## PipelineTextInputDefinition

Text pipeline input definition

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this input — used in &lt;&lt; pipeline.input.key &gt;&gt; templates _(minLen:1)_ |
| `type` | text | Yes | — | Input type — determines the field widget and validation rules |
| `required` | bool | No | `true` | Whether this input must be provided when running the pipeline |
| `description` | str | No | — | Human-readable description shown in the UI when running the pipeline |
| `default` | str | No | — | Default value used when no value is provided at runtime |
| `patterns` | [Module.ValidationPattern](#module-validationpattern)[] | No | — | Regex patterns the value must match |

## GenericPipelineTrigger

Pipeline trigger for generic webhooks.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this trigger |
| `repo` | str | Yes | — | Git repository in owner/repo format _(templateable)_ |
| `filter` | [TriggerFilter](#triggerfilter) | No | — | Filters to narrow which events trigger the pipeline |
| `run` | map&lt;str,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; | Yes | — | Maps variant IDs to run configuration — keys must match declared variant IDs. |
| `type` | webhook | Yes | — | Webhook type for generic webhook triggers. |

## GithubPipelineTrigger

Pipeline trigger for GitHub webhooks.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this trigger |
| `repo` | str | Yes | — | Git repository in owner/repo format _(templateable)_ |
| `filter` | [TriggerFilter](#triggerfilter) | No | — | Filters to narrow which events trigger the pipeline |
| `run` | map&lt;str,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; | Yes | — | Maps variant IDs to run configuration — keys must match declared variant IDs. |
| `type` | "webhook:github" | Yes | — | Webhook type — determines which events are supported. |
| `event` | enum[push,pull_request,pull_request_review,pull_request_review_comment,pull_request_review_thread,release,deployment] | Yes | — | Webhook event type to listen for. |

## GitlabPipelineTrigger

Pipeline trigger for GitLab webhooks.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique identifier for this trigger |
| `repo` | str | Yes | — | Git repository in owner/repo format _(templateable)_ |
| `filter` | [TriggerFilter](#triggerfilter) | No | — | Filters to narrow which events trigger the pipeline |
| `run` | map&lt;str,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; | Yes | — | Maps variant IDs to run configuration — keys must match declared variant IDs. |
| `type` | "webhook:gitlab" | Yes | — | Webhook type — determines which events are supported. |
| `event` | enum[push,merge_request,tag_push] | Yes | — | Webhook event type to listen for. |

## ActionStep

Union of all action step types.

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `approval` | [ApprovalStep](#approvalstep) |
| `build` | [BuildStep](#buildstep) |
| `"build:image"` | [BuildImageCiStep](#buildimagecistep) |
| `"build:static"` | [BuildStaticCiStep](#buildstaticcistep) |
| `custom` | [CustomCommandCiStep](#customcommandcistep) |
| `deploy` | [DeployStep](#deploystep) |
| `"deploy:swap-blue-green"` | [DeploySwapBlueGreenStep](#deployswapbluegreenstep) |
| `rollback` | [RollbackStep](#rollbackstep) |
| `sleep` | [SleepStep](#sleepstep) |
| `"terraform:apply"` | [TerraformApplyCiStep](#terraformapplycistep) |
| `"terraform:plan"` | [TerraformPlanCiStep](#terraformplancistep) |
| `"trigger:pipeline"` | [TriggerPipelineStep](#triggerpipelinestep) |

## GroupStep

Group step - contains nested steps

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `group` | str | Yes | — | Human-readable group label shown in pipeline UI |
| `steps` | [Step](#step)[] | Yes | — | _(minItems:1)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for executions within this group |

## ParallelStep

Parallel step - executes children concurrently

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `parallel` | [ParallelChild](#parallelchild)[] | Yes | — | _(minItems:1)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for executions within this parallel block |
| `fail_strategy` | enum[cancel-all,finish-running,finish-all] | No | `cancel-all` | How to handle failures while branches in this parallel block are running |

## Module.NumberValueItem

A selectable option with a numeric value.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `label` | str | No | — | Text shown in the dropdown |
| `description` | str | No | — | Extra detail shown alongside the option |
| `group_label` | str | No | — | Groups options under a shared heading in the dropdown |
| `value` | int | Yes | — | Actual number sent to Terraform when this option is selected _(format:int32)_ |

## Module.ValidationPattern

A regex pattern with an error message for validation

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `pattern` | str | Yes | — | JavaScript regex the input must match |
| `message` | str | Yes | — | Error shown to the user when validation fails |

## Module.StringValueItem

A selectable option with a string value.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `label` | str | No | — | Text shown in the dropdown |
| `description` | str | No | — | Extra detail shown alongside the option |
| `group_label` | str | No | — | Groups options under a shared heading in the dropdown |
| `value` | str | Yes | — | Actual value sent to Terraform when this option is selected |

## TriggerFilter

Trigger filter configuration

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `branch` | str \| null | No | — | Only trigger when this branch is pushed _(templateable)_ |
| `tag` | str \| null | No | — | Only trigger when a tag matching this pattern is pushed _(templateable)_ |
| `action` | str \| str[] | No | — | Only trigger for specific webhook actions. _(templateable)_ |
| `paths` | str[] \| null | No | — | Only trigger when files in these paths are changed _(templateable)_ |
| `payload` | [PayloadCondition](#payloadcondition)[] | No | — | Filter by webhook payload fields using dot-notation paths. |

## TriggerRunVariantConfig

Per-variant run configuration for a trigger.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `description` | str \| null | No | — | Description for the pipeline run created by this trigger variant _(templateable)_ |
| `input` | map&lt;str,any \| null&gt; | No | — | Input overrides for this variant run _(templateable)_ |

## GithubWebhookEvent

GitHub webhook events that can trigger a pipeline

Type: enum[push,pull_request,pull_request_review,pull_request_review_comment,pull_request_review_thread,release,deployment]

## GitlabWebhookEvent

GitLab webhook events that can trigger a pipeline

Type: enum[push,merge_request,tag_push]

## ApprovalStep

Approval action step — pauses pipeline execution until a user explicitly approves

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | approval | Yes | — | — |

## BuildImageCiStep

Build image CI action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | "build:image" | Yes | — | — |
| `source` | [GitSource](#gitsource) | Yes | — | Git source configuration _(templateable)_ |
| `builder` | [ImageBuildConfig](#imagebuildconfig) | Yes | — | Builder configuration — dockerfile or nixpacks settings for image builds _(templateable)_ |
| `environment_variables` | [EnvironmentVariablesValue](#environmentvariablesvalue) | No | — | Environment variables available during the build — plain strings or AWS secret references _(templateable)_ |
| `debug` | bool \| null | No | — | Enable verbose build output for debugging build issues Accepts either a boolean literal or a full template token _(templateable)_ |
| `destinations` | [EcrDestination](#ecrdestination)[] | Yes | — | ECR destinations where the built Docker image will be pushed _(minItems:1; templateable)_ |
| `infrastructure` | [StepInfrastructure](#stepinfrastructure) | Yes | — | Execution environment configuration — required because image builds run on provisioned compute _(templateable)_ |

## BuildStaticCiStep

Build static CI action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | "build:static" | Yes | — | — |
| `source` | [GitSource](#gitsource) | Yes | — | Git source configuration _(templateable)_ |
| `builder` | [StaticBuildConfig](#staticbuildconfig) | Yes | — | Builder configuration — dockerfile or nixpacks settings _(templateable)_ |
| `environment_variables` | [EnvironmentVariablesValue](#environmentvariablesvalue) | No | — | Environment variables available during the build — plain strings or AWS secret references _(templateable)_ |
| `debug` | bool \| null | No | — | Enable verbose build output for debugging build issues Accepts either a boolean literal or a full template token _(templateable)_ |
| `destinations` | [S3StaticDestination](#s3staticdestination)[] | Yes | — | S3 destinations where the built static assets will be uploaded _(minItems:1; templateable)_ |
| `infrastructure` | [StepInfrastructure](#stepinfrastructure) | Yes | — | Execution environment configuration — required because static builds run on provisioned compute _(templateable)_ |

## BuildStep

General-purpose build action step — triggers a build based on the module's build config.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `module_instance` | str | Yes | — | Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_ |
| `description` | str \| null | No | — | Human-readable description of this deployment. _(templateable)_ |
| `type` | build | Yes | — | — |
| `input` | map&lt;str,any \| null&gt; | No | — | User-defined build parameters passed through to the module build manager. _(templateable)_ |

## CustomCommandCiStep

Custom command CI action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `setup` | [SetupAction](#setupaction)[] \| null | No | — | Setup actions to install tools before the commands run, in order, before any command. _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | custom | Yes | — | — |
| `source` | [GitSource](#gitsource) | No | — | Git source configuration _(templateable)_ |
| `commands` | str[] | Yes | — | Shell commands to execute in order _(minItems:1; templateable)_ |
| `environment_variables` | [EnvironmentVariablesValue](#environmentvariablesvalue) | No | — | Environment variables available during command execution _(templateable)_ |
| `infrastructure` | [StepInfrastructure](#stepinfrastructure) | Yes | — | Execution environment configuration — required because custom commands run on provisioned compute _(templateable)_ |

## DeployStep

General-purpose deploy action step — triggers a deployment based on the module's deployment config.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `module_instance` | str | Yes | — | Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_ |
| `description` | str \| null | No | — | Human-readable description of this deployment. _(templateable)_ |
| `type` | deploy | Yes | — | — |
| `input` | map&lt;str,any \| null&gt; | No | — | User-defined deploy parameters passed through to the deploy manager. _(templateable)_ |

## DeploySwapBlueGreenStep

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `module_instance` | str | Yes | — | Module instance reference — accepts the module instance row ID (e.g. `mi_…`), `environmentGivenId.moduleGivenId`, or `projectGivenId.environmentGivenId.modul... _(minLen:1; templateable)_ |
| `description` | str \| null | No | — | Human-readable description of this deployment. _(templateable)_ |
| `type` | "deploy:swap-blue-green" | Yes | — | — |
| `input` | map&lt;str,any \| null&gt; | No | — | _(templateable)_ |

## RollbackStep

Rollback action step — reverts a module to a specific previous deployment.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `type` | rollback | Yes | — | — |
| `deployment_id` | str | Yes | — | Deployment ID to rollback to _(minLen:1; templateable)_ |

## SleepStep

Sleep action step — pauses pipeline execution for testing and debugging

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | sleep | Yes | — | — |
| `duration_ms` | int | Yes | — | Sleep duration in milliseconds _(format:int64; templateable)_ |

## TerraformApplyCiStep

Terraform apply CI action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | "terraform:apply" | Yes | — | — |
| `source` | [GitSource](#gitsource) | No | — | Git source configuration — if omitted, uses the trigger's repository _(templateable)_ |
| `environment_variables` | [EnvironmentVariablesValue](#environmentvariablesvalue) | No | — | Environment variables available during execution — plain strings or AWS secret references _(templateable)_ |
| `tool` | enum[opentofu,terraform] | Yes | — | IaC tool implementation used to execute this step _(templateable)_ |
| `version` | str | Yes | — | Terraform/OpenTofu version to execute _(minLen:1; templateable)_ |
| `stack_id` | str \| null | No | — | Stack ID used for terraform resource/execution tracking Automatically set when triggered from a stack _(templateable)_ |
| `plan_file_uri` | str | Yes | — | S3 URI of the plan file to apply — typically from a preceding plan step output (e.g. s3://bucket/path/to/plan.tfplan) _(templateable)_ |
| `infrastructure` | [StepInfrastructure](#stepinfrastructure) | Yes | — | Execution environment configuration — required because Terraform apply runs on provisioned compute _(templateable)_ |

## TerraformPlanCiStep

Terraform plan CI action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | "terraform:plan" | Yes | — | — |
| `source` | [GitSource](#gitsource) | No | — | Git source configuration — if omitted, uses the trigger's repository _(templateable)_ |
| `environment_variables` | [EnvironmentVariablesValue](#environmentvariablesvalue) | No | — | Environment variables available during execution — plain strings or AWS secret references _(templateable)_ |
| `tool` | enum[opentofu,terraform] | Yes | — | IaC tool implementation used to execute this step _(templateable)_ |
| `version` | str | Yes | — | Terraform/OpenTofu version to execute _(minLen:1; templateable)_ |
| `stack_id` | str \| null | No | — | Stack ID used for terraform resource/execution tracking Automatically set when triggered from a stack _(templateable)_ |
| `terraform_variables` | [TerraformVariablesValue](#terraformvariablesvalue) | No | — | Terraform input variables written to a .auto.tfvars.json file during plan creation — supports all HCL types and AWS secret references _(templateable)_ |
| `terraform_variable_files` | str[] \| null | No | — | Terraform tfvars file paths passed to terraform plan as -var-file flags, in the configured order. _(templateable)_ |
| `plan_file_directory_uri` | str \| null | No | — | S3 directory URI to upload the plan file to — overrides the default convention-based path (e.g. s3://bucket/terraform-plans/run-123) _(templateable)_ |
| `plan_type` | enum[apply,destroy] | No | `apply` | Type of plan to generate — "apply" (default) creates/updates resources, "destroy" tears them down _(templateable)_ |
| `infrastructure` | [StepInfrastructure](#stepinfrastructure) | Yes | — | Execution environment configuration — required because Terraform plan runs on provisioned compute _(templateable)_ |

## TriggerPipelineStep

Trigger pipeline action step

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique step identifier, used for referencing in logs and dependencies _(minLen:1)_ |
| `name` | str | No | — | Human-readable step name shown in the pipeline UI |
| `timeout` | int \| null | No | — | Maximum execution time in seconds before the step is killed _(format:int32,min:1; templateable)_ |
| `if` | bool \| null | No | — | Condition that must evaluate to true for this step to run Accepts either a boolean literal or a full template token that resolves to a boolean _(templateable)_ |
| `concurrency` | [ConcurrencyConfig](#concurrencyconfig) | No | — | Concurrency configuration for controlling step execution across variants |
| `type` | "trigger:pipeline" | Yes | — | — |
| `pipeline_id` | str | Yes | — | ID of the pipeline to trigger _(minLen:1; templateable)_ |
| `pipeline_version_id` | str \| null | No | — | Specific pipeline version to trigger _(templateable)_ |
| `description` | str | Yes | — | Description to use for the triggered pipeline run _(minLen:1; templateable)_ |
| `inputs` | map&lt;str,any \| null&gt; | No | — | Input values to pass to the triggered pipeline as key-value pairs _(templateable)_ |
| `run` | map&lt;str,[TriggerRunVariantConfig](#triggerrunvariantconfig)&gt; \| null | No | — | Per-variant run overrides — keys are variant IDs, values include description and optional input overrides _(templateable)_ |

## ConcurrencyConfig

Concurrency configuration for controlling parallel execution

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `key` | str | No | — | Unique identifier for the concurrency scope within this pipeline Steps/blocks with the same key share a concurrency limit Keys are scoped to org + pipeline —... |
| `value` | int | No | `0` | Maximum concurrent executions allowed (0 means unlimited, 1 means sequential) _(format:int32,min:0)_ |
| `behavior` | enum[queue,cancel-in-progress] | No | `queue` | Behavior when the concurrency limit is reached |
| `queue_size` | int | No | — | Maximum queue size when behavior is "queue" Only applies when behavior is "queue" Unset means unlimited queue depth 0 disables queueing when behavior is "queue" _(format:int32,min:0)_ |
| `queue_overflow_discard` | enum[oldest,newest] | No | `oldest` | Which items to discard when queue overflows Only applies when behavior is "queue" and queue_size &gt; 0 |

## ParallelChild

Parallel child - can be action or group (no nested parallel allowed)

One of: [ActionStep](#actionstep), [GroupStep](#groupstep).

## ParallelFailStrategy

Strategy for handling failures within a parallel block

Type: enum[cancel-all,finish-running,finish-all]

## PayloadCondition

Condition to check against a webhook payload field.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `path` | str | Yes | — | Dot-notation path to the payload field |
| `equals` | str \| null | No | — | Exact string match _(templateable)_ |
| `contains` | str \| null | No | — | Substring match _(templateable)_ |
| `matches` | str \| null | No | — | Glob pattern match _(templateable)_ |
| `one_of` | str[] \| null | No | — | Value must be one of these strings _(templateable)_ |
| `not` | [PayloadConditionMatcher](#payloadconditionmatcher) | No | — | Negated payload field matcher. The condition fails when this matcher is true. |

## EcrDestination

ECR destination for image builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique destination identifier within the build step _(templateable)_ |
| `type` | ecr | Yes | — | ECR destination type _(templateable)_ |
| `repository_arn` | str | Yes | — | ECR repository ARN _(templateable)_ |
| `tags` | str[] \| null | No | — | Docker image tags to apply in ECR after the build finishes _(templateable)_ |

## EnvironmentVariablesValue

Environment variable map value.

One of: map&lt;str,[EnvironmentVariable](#environmentvariable)&gt;, str.

## GitSource

Git source configuration for fetching code

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | git | Yes | — | Source type identifier _(templateable)_ |
| `repo` | str | Yes | — | Full repository URL including host _(templateable)_ |
| `branch` | str | Yes | — | Branch to check out _(minLen:1; templateable)_ |
| `ref` | str \| null | No | — | Git ref to check out — branch, commit SHA, tag, or other ref. Overrides branch HEAD if set. _(templateable)_ |
| `base_path` | str \| null | No | `.` | Base path within the repository to use as the working root _(templateable)_ |

## ImageBuildConfig

Image build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `disabled` | [DisabledBuildConfig](#disabledbuildconfig) |
| `dockerfile` | [ImageDockerfileBuildConfig](#imagedockerfilebuildconfig) |
| `nixpacks` | [ImageNixpacksBuildConfig](#imagenixpacksbuildconfig) |

## StepInfrastructure

Step infrastructure configuration

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `execution_environment_id` | str \| null | No | — | ID or given ID of a pre-configured execution environment _(templateable)_ |
| `aws_account_id` | str \| null | No | — | AWS account reference for automatic network selection. _(templateable)_ |
| `region` | str \| null | No | — | AWS region for automatic network selection _(templateable)_ |
| `type` | enum[ec2,ec2-spot] | Yes | — | Instance type — "ec2" for on-demand or "ec2-spot" for cheaper spot instances _(templateable)_ |
| `instance_size` | str \| str[] | Yes | — | Instance size — a single size like "large" or an array like ["large", "xlarge"] for spot fallback _(templateable)_ |
| `ami` | str \| null | No | — | AMI ID for the runner instance _(templateable)_ |
| `permissions` | [AwsPermissions](#awspermissions) | No | — | IAM permissions for the runner role — "attach" to add policies or "replace" to override step defaults _(templateable)_ |
| `storage` | [StorageConfig](#storageconfig) | No | — | Root volume storage configuration _(templateable)_ |

## S3StaticDestination

S3 destination for static builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | str | Yes | — | Unique destination identifier within the build step _(minLen:1; templateable)_ |
| `type` | s3 | Yes | — | S3 destination type _(templateable)_ |
| `bucket` | str | Yes | — | S3 bucket name where built assets will be uploaded _(templateable)_ |
| `directory` | str \| null | No | — | S3 directory override for the uploaded build output. _(templateable)_ |
| `region` | str | Yes | — | AWS region for the destination S3 bucket _(templateable)_ |

## StaticBuildConfig

Static build configuration — discriminated by build method

Discriminated union. The `type` field selects which schema applies.

| `type` value | Schema |
| --- | --- |
| `dockerfile` | [StaticDockerfileBuildConfig](#staticdockerfilebuildconfig) |
| `nixpacks` | [StaticNixpacksBuildConfig](#staticnixpacksbuildconfig) |

## SetupAction

A setup action that installs a tool before the step's commands run.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `uses` | str | Yes | — | Action reference to install a tool before running commands. _(minLen:1; templateable)_ |
| `with` | map&lt;str,str&gt; \| null | No | — | Inputs passed to the action, keyed by the action's declared input names. _(templateable)_ |

## TerraformTool

Supported IaC tool for terraform:* CI steps

Type: enum[opentofu,terraform]

## TerraformPlanType

Type of Terraform plan to generate

Type: enum[apply,destroy]

## TerraformVariableFilePath

Terraform/OpenTofu tfvars file path relative to the step source base path

Type: str

## TerraformVariablesValue

Terraform variable map value.

One of: map&lt;str,[TerraformVariable](#terraformvariable)&gt;, str.

## ConcurrencyBehavior

Behavior when the concurrency limit is reached

Type: enum[queue,cancel-in-progress]

## QueueOverflowDiscard

Queue overflow discard strategy

Type: enum[oldest,newest]

## PayloadConditionMatcher

Payload field matcher used by positive conditions and negated conditions.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `equals` | str \| null | No | — | Exact string match _(templateable)_ |
| `contains` | str \| null | No | — | Substring match _(templateable)_ |
| `matches` | str \| null | No | — | Glob pattern match _(templateable)_ |
| `one_of` | str[] \| null | No | — | Value must be one of these strings _(templateable)_ |

## EnvironmentVariable

Environment variable — can be a plain string, Parameter Store reference, or Secrets Manager reference

One of: str \| null, [ParameterStoreEnvVar](#parameterstoreenvvar), [SecretsManagerEnvVar](#secretsmanagerenvvar).

## GitRepoUrl

Full git repository URL including host.

Type: str

## DisabledBuildConfig

Disabled build configuration. Valid only for module-driven builds.

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | disabled | Yes | — | Build method — disables module build execution _(templateable)_ |

## ImageDockerfileBuildConfig

Dockerfile configuration for image builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | dockerfile | Yes | — | Build method — use a custom Dockerfile _(templateable)_ |
| `dockerfile` | str \| null | No | `Dockerfile` | Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_ |
| `context` | str \| null | No | `.` | Docker build context path relative to source.base_path _(templateable)_ |
| `inject_env_variables_in_dockerfile` | bool \| null | No | — | Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_ |
| `cache_from` | [CacheFromConfig](#cachefromconfig) | No | — | Docker layer cache configuration _(templateable)_ |

## ImageNixpacksBuildConfig

Nixpacks configuration for image builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | nixpacks | Yes | — | Build method — use Nixpacks auto-detection _(templateable)_ |
| `nixpacks_version` | str \| null | No | — | Nixpacks version to use _(templateable)_ |
| `build_path` | str \| null | No | — | Directory to run the Nixpacks build from, relative to the repository root _(templateable)_ |
| `config_file_path` | str \| null | No | — | Path to the Nixpacks configuration file relative to the repository root _(templateable)_ |
| `nix_pkgs` | str[] \| null | No | — | Additional Nix packages to install _(templateable)_ |
| `apt_pkgs` | str[] \| null | No | — | Additional apt packages to install _(templateable)_ |
| `nix_libs` | str[] \| null | No | — | Additional Nix libraries to install _(templateable)_ |
| `install_cmd` | str \| null | No | — | Custom install command _(templateable)_ |
| `build_cmd` | str \| null | No | — | Custom build command _(templateable)_ |
| `start_cmd` | str \| null | No | — | Application start command _(templateable)_ |
| `cache_from` | [CacheFromConfig](#cachefromconfig) | No | — | Docker layer cache configuration _(templateable)_ |

## AwsPermissions

IAM policies configuration for runner roles Used by EC2-backed steps to specify additional permissions

One of: [AwsPermissionsAttach](#awspermissionsattach), [AwsPermissionsReplace](#awspermissionsreplace).

## InfrastructureType

Execution environment type

Type: enum[ec2,ec2-spot]

## InstanceSize

Instance size can be a single string or array of strings

One of: str, str[].

## StorageConfig

Root volume storage configuration

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | enum[gp3,gp2,io2,io1] | No | — | Volume type _(templateable)_ |
| `size` | int \| null | No | — | Volume size in GiB _(format:int32; templateable)_ |
| `iops` | int \| null | No | — | Provisioned IOPS _(format:int32; templateable)_ |
| `throughput` | int \| null | No | — | Provisioned throughput in MiB/s _(format:int32; templateable)_ |

## StaticDockerfileBuildConfig

Dockerfile configuration for static builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | dockerfile | Yes | — | Build method — use a custom Dockerfile _(templateable)_ |
| `dockerfile` | str \| null | No | `Dockerfile` | Path to Dockerfile relative to source.base_path _(minLen:1; templateable)_ |
| `context` | str \| null | No | `.` | Docker build context path relative to source.base_path _(templateable)_ |
| `inject_env_variables_in_dockerfile` | bool \| null | No | — | Whether to inject environment variables as Docker build args Accepts either a boolean literal or a full template token _(templateable)_ |
| `output_directory` | str | Yes | — | Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_ |

## StaticNixpacksBuildConfig

Nixpacks configuration for static builds

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `type` | nixpacks | Yes | — | Build method — use Nixpacks auto-detection _(templateable)_ |
| `nixpacks_version` | str \| null | No | — | Nixpacks version to use _(templateable)_ |
| `build_path` | str \| null | No | — | Directory to run the Nixpacks build from, relative to the repository root _(templateable)_ |
| `config_file_path` | str \| null | No | — | Path to the Nixpacks configuration file relative to the repository root _(templateable)_ |
| `nix_pkgs` | str[] \| null | No | — | Additional Nix packages to install _(templateable)_ |
| `apt_pkgs` | str[] \| null | No | — | Additional apt packages to install _(templateable)_ |
| `nix_libs` | str[] \| null | No | — | Additional Nix libraries to install _(templateable)_ |
| `install_cmd` | str \| null | No | — | Custom install command _(templateable)_ |
| `build_cmd` | str \| null | No | — | Custom build command _(templateable)_ |
| `output_directory` | str | Yes | — | Directory inside the built image containing the static assets to upload This path is resolved relative to source.base_path _(minLen:1; templateable)_ |

## TerraformVariable

Terraform variable — can be any JSON value (string, number, boolean, null, array, object), a Parameter Store reference, or a Secrets Manager reference

One of: [ParameterStoreEnvVar](#parameterstoreenvvar), [SecretsManagerEnvVar](#secretsmanagerenvvar), any \| null.

## ParameterStoreEnvVar

Environment variable from AWS Parameter Store

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `from_parameter_store` | [ParameterStoreReferenceValue](#parameterstorereferencevalue) | Yes | — | Parameter Store reference _(templateable)_ |

## SecretsManagerEnvVar

Environment variable from AWS Secrets Manager

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `from_secrets_manager` | [SecretsManagerReferenceValue](#secretsmanagerreferencevalue) | Yes | — | Secrets Manager reference _(templateable)_ |

## CacheFromConfig

Configuration for Docker layer cache source from ECR

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `digest` | str \| null | No | — | Specific image digest to use for cache _(templateable)_ |
| `tag` | str \| null | No | — | Specific image tag to use for cache _(templateable)_ |

## AwsPermissionsAttach

IAM policies to attach alongside step default policies

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `attach` | str[] | Yes | — | AWS managed policy ARNs to attach alongside step default policies _(templateable)_ |

## AwsPermissionsReplace

IAM policies to replace step default policies entirely

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `replace` | str[] | Yes | — | AWS managed policy ARNs to use instead of step default policies _(templateable)_ |

## EbsVolumeType

EBS volume type for the runner root volume

Type: enum[gp3,gp2,io2,io1]

## ParameterStoreReferenceValue

Parameter Store reference — shorthand string or object with optional version metadata

One of: str, [ParameterStoreReference](#parameterstorereference).

## SecretsManagerReferenceValue

Secrets Manager reference — shorthand string or object with optional JSON key/version metadata

One of: str, [SecretsManagerReference](#secretsmanagerreference).

## ParameterStoreReference

Parameter Store reference with optional version metadata

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `key` | str | Yes | — | Parameter Store parameter name or path _(templateable)_ |
| `version` | str \| null | No | — | Parameter version number _(templateable)_ |

## SecretsManagerReference

Secrets Manager reference with optional JSON key and version metadata

| Field | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `key` | str | Yes | — | Secret name or ARN in Secrets Manager _(templateable)_ |
| `json_key` | str \| null | No | — | JSON key to extract from a JSON-structured secret _(templateable)_ |
| `version` | str \| null | No | — | Secret version ID or stage _(templateable)_ |

## Template expressions

Fields marked _templateable_ accept `<< expression >>` template tokens.

- Block mode preserves the resolved type:

  ```yaml
  timeout: << pipeline.input.timeout >>
  ```

- Interpolation mode always produces a string:

  ```yaml
  name: "prefix-<< pipeline.input.env >>"
  ```

Supported operators: `+`, `-`, `*`, `/`, `%`, `>`, `<`, `>=`, `<=`, `==`, `!=`, `&&`, `||`, `!`.

Ternary: `<< condition ? trueVal : falseVal >>`. Default fallback: `` << input.x || "fallback" >> ``.

Access: `array[0]`, `map["key"]`, `map.key`. Functions: `len()`, `upper()`, `lower()`.

Available namespaces: `pipeline.input.*`, `pipeline.run.{id,description}`, `pipeline.variant.{id,name}`, `steps.{id}.{output,input}.*`, `step.input.*`, `trigger.payload.{branch,commit,ref,repository,event,tag}`.
