export const DeployPrerequisites = () => (
  <>
    <h2 id="prerequisites">Prerequisites</h2>
    <ul>
      <li>A Git repository containing your application and its production build command.</li>
      <li>A Ravion account with your AWS account connected. Start at <a href="https://app.ravion.com/signup">Ravion signup</a>.</li>
      <li>The Ravion CLI installed and authenticated. See <a href="/cli/installation">CLI installation</a>.</li>
    </ul>
    <p>
      The examples use <code>my-org/my-app</code> as a placeholder. Replace it with your
      repository before applying the config.
    </p>
  </>
);
